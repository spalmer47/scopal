---
phase: 03-firm-substance-people-practice-areas-pricing
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - public/images/team/scott-palmer.png
  - src/assets/team/scott-palmer.jpg
autonomous: false
requirements: [TEAM-01, TEAM-02]

must_haves:
  truths:
    - "Scott's headshot exists at src/assets/team/ at ≤200 KB for use with astro:assets Image component"
    - "Rachel's headshot exists at src/assets/team/ at ≤200 KB"
    - "Execution is not blocked by missing image assets"
  artifacts:
    - path: "src/assets/team/scott-palmer.jpg"
      provides: "Scott's optimized headshot for bio page"
      max_size_kb: 200
    - path: "src/assets/team/rachel-palmer.jpg"
      provides: "Rachel's headshot for team page"
      max_size_kb: 200
  key_links:
    - from: "src/pages/attorneys/scott-palmer.astro"
      to: "src/assets/team/scott-palmer.jpg"
      via: "import scottHeadshot from '../../assets/team/scott-palmer.jpg'"
      pattern: "assets/team/scott-palmer"
    - from: "src/pages/team/rachel-palmer.astro"
      to: "src/assets/team/rachel-palmer.jpg"
      via: "import rachelHeadshot from '../../assets/team/rachel-palmer.jpg'"
      pattern: "assets/team/rachel-palmer"
---

<objective>
Unblock Phase 3 execution by ensuring both headshots exist at the correct location and size.

Purpose: Scott's headshot exists but is 296 KB — above the 200 KB source ceiling enforced by PERF-01 and the project's non-negotiable image rule. Rachel's headshot does not exist in the repository at all. No page in Phase 3 can build correctly until both files are present in src/assets/team/ at ≤200 KB.

Output: Two optimized headshot files in src/assets/team/. Execution agent (Wave 1) unblocked.
</objective>

<execution_context>
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/phases/03-firm-substance-people-practice-areas-pricing/03-00-PLAN.md
</execution_context>

<context>
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/FIRM_BRIEF.md
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize Scott's headshot and create src/assets/team/ directory</name>
  <files>src/assets/team/scott-palmer.jpg</files>
  <read_first>
    - public/images/team/scott-palmer.png (the oversized source — read its current size before touching it)
  </read_first>
  <action>
    1. Create the directory src/assets/team/ if it does not exist.
    2. Check the size of public/images/team/scott-palmer.png:
       Run: du -k "public/images/team/scott-palmer.png"
       Expected output: ~296 (KB). This confirms the file exceeds the 200 KB ceiling.
    3. Optimize and convert to JPEG using macOS sips:
       Run: sips -s format jpeg -s formatOptions 82 "public/images/team/scott-palmer.png" --out "src/assets/team/scott-palmer.jpg"
       (Quality 82 typically yields ~100–160 KB for a 400x400 headshot portrait. Adjust down to 75 if still over 200 KB.)
    4. Verify the output file size:
       Run: du -k "src/assets/team/scott-palmer.jpg"
       Must be ≤200. If above 200, re-run sips with --resampleWidth 800 (if wider than 800px) or reduce formatOptions to 70.
    5. Do NOT delete public/images/team/scott-palmer.png — leave the original in place. Only the src/assets/team/ version will be used by the Image component.
  </action>
  <verify>
    <automated>test -f "src/assets/team/scott-palmer.jpg" && echo "FILE_EXISTS" && du -k "src/assets/team/scott-palmer.jpg" | awk '{print $1}' | awk '$1 &lt;= 200 {print "SIZE_OK"}'</automated>
  </verify>
  <acceptance_criteria>
    - src/assets/team/scott-palmer.jpg exists
    - du -k src/assets/team/scott-palmer.jpg reports ≤200 (KB)
    - File is a valid JPEG (sips exits 0)
  </acceptance_criteria>
  <done>src/assets/team/scott-palmer.jpg exists and is ≤200 KB</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Scott commits Rachel's headshot</name>
  <what-built>
    The src/assets/team/ directory now exists with Scott's optimized headshot. Rachel's headshot slot is empty — this file does not exist anywhere in the repository. No automation can create a real headshot photo.
  </what-built>
  <how-to-verify>
    1. Find Rachel's headshot photo (professional photo preferred; can be the same session as Scott's).
    2. If the file is larger than 200 KB, run this command in Terminal to resize it:
       sips -s format jpeg -s formatOptions 82 "/path/to/rachel-photo.jpg" --out "src/assets/team/rachel-palmer.jpg"
       Replace /path/to/rachel-photo.jpg with the actual path to the photo file.
    3. If the file is already ≤200 KB and is a JPEG or WebP, simply copy it to:
       src/assets/team/rachel-palmer.jpg
       (or rachel-palmer.webp — either extension works)
    4. Verify the file is in place by running:
       du -k "src/assets/team/rachel-palmer.jpg"
       The number shown must be 200 or less.
    5. Add and commit the file:
       git add src/assets/team/rachel-palmer.jpg
       git commit -m "feat(03): add Rachel Palmer headshot"
  </how-to-verify>
  <resume-signal>
    Type "headshots ready" once both src/assets/team/scott-palmer.jpg AND src/assets/team/rachel-palmer.jpg are committed. The execution agent will verify both files exist and are ≤200 KB before proceeding to Wave 1.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local file → repository | Image files committed to git; must not exceed 200 KB ceiling or violate project rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00-01 | Information Disclosure | scott-palmer.jpg EXIF metadata | accept | Astro's Image component strips EXIF on build output; source EXIF in src/assets/ is not publicly served |
</threat_model>

<verification>
Run after both headshots are committed:

```bash
du -k src/assets/team/scott-palmer.jpg
du -k src/assets/team/rachel-palmer.jpg
```

Both must report ≤200. If either reports above 200, re-optimize before proceeding to Wave 1.
</verification>

<success_criteria>
- src/assets/team/scott-palmer.jpg exists and is ≤200 KB
- src/assets/team/rachel-palmer.jpg exists and is ≤200 KB
- Both files are committed to git
- npm run build does not error on missing asset imports (Wave 1 will add those imports)
</success_criteria>

<output>
After completion, create .planning/phases/03-firm-substance-people-practice-areas-pricing/03-00-SUMMARY.md
</output>
