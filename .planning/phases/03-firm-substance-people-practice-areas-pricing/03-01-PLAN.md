---
phase: 03-firm-substance-people-practice-areas-pricing
plan: "01"
type: execute
wave: 1
depends_on: ["03-00"]
files_modified:
  - content.config.ts
  - src/components/layout/Header.astro
  - src/components/seo/FAQSchema.astro
  - src/components/seo/BreadcrumbSchema.astro
  - src/components/seo/PersonSchema.astro
  - src/components/seo/ServiceSchema.astro
  - src/components/legal/DisclaimerCallout.astro
  - src/layouts/PracticeAreaLayout.astro
autonomous: true
requirements: [PRAC-04, PRAC-05, TEAM-03]

must_haves:
  truths:
    - "The coaching content collection is registered in Astro so coaching MDX files will be recognized at build time"
    - "Header shows Practice Areas dropdown (Corporate Law, Fractional GC) and a Coaching top-level link in nav order: Practice Areas dropdown | Coaching | Scott Palmer | Pricing | Book a Fit Call"
    - "PracticeAreaLayout auto-injects disclaimer callout, FAQPage schema, BreadcrumbList schema, Service schema, and CTASection without page authors wiring them"
    - "FAQSchema, BreadcrumbSchema, PersonSchema, ServiceSchema, and DisclaimerCallout components exist and follow the established set:html JSON-LD pattern"
    - "npm run build exits 0 after this plan (no missing imports, no broken schema)"
  artifacts:
    - path: "content.config.ts"
      provides: "coaching collection definition + updated export"
      contains: "coaching"
    - path: "src/layouts/PracticeAreaLayout.astro"
      provides: "Auto-injecting layout for practice area pages"
      exports: []
    - path: "src/components/seo/FAQSchema.astro"
      provides: "FAQPage JSON-LD emitter"
    - path: "src/components/seo/BreadcrumbSchema.astro"
      provides: "BreadcrumbList JSON-LD emitter"
    - path: "src/components/seo/PersonSchema.astro"
      provides: "Person JSON-LD emitter for Scott's bio"
    - path: "src/components/seo/ServiceSchema.astro"
      provides: "Service JSON-LD emitter for practice areas"
    - path: "src/components/legal/DisclaimerCallout.astro"
      provides: "Page-level attorney-client disclaimer callout box"
    - path: "src/components/layout/Header.astro"
      provides: "Updated nav with Practice Areas dropdown + Coaching link"
  key_links:
    - from: "src/layouts/PracticeAreaLayout.astro"
      to: "src/components/seo/FAQSchema.astro"
      via: "import FAQSchema"
      pattern: "FAQSchema"
    - from: "src/layouts/PracticeAreaLayout.astro"
      to: "src/components/sections/CTASection.astro"
      via: "import CTASection"
      pattern: "CTASection"
    - from: "src/components/layout/Header.astro"
      to: "/practice-areas/corporate-law"
      via: "anchor href in dropdown"
      pattern: "practice-areas/corporate-law"
---

<objective>
Build the shared infrastructure that all Phase 3 content pages depend on: the coaching collection registration, four schema components, the DisclaimerCallout, PracticeAreaLayout, and the updated Header with Practice Areas dropdown.

Purpose: Practice area pages, the coaching page, and the bio page all import from these shared pieces. Creating them first (Wave 1) means Wave 2 content pages can import and use them without guesswork.

Output: 8 files created or modified. npm run build exits 0. The infrastructure layer is complete and ready for Wave 2 content.
</objective>

<execution_context>
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/phases/03-firm-substance-people-practice-areas-pricing/03-01-PLAN.md
</execution_context>

<context>
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/FIRM_BRIEF.md
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/ROADMAP.md
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/phases/03-firm-substance-people-practice-areas-pricing/03-CONTEXT.md
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/phases/03-firm-substance-people-practice-areas-pricing/03-RESEARCH.md
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/phases/03-firm-substance-people-practice-areas-pricing/03-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Read these files first. -->

From content.config.ts (current state — executor must read this before editing):
```ts
import { defineCollection, z } from 'astro:content';
import { glob } from 'astro/loaders';
// blog, practiceAreas, team collections already defined
export const collections = { blog, practiceAreas, team };
// MUST become: export const collections = { blog, practiceAreas, team, coaching };
```

From src/lib/constants.ts:
```ts
export const FIRM = { legalName, shortName, url, email, address, areaServed, noAttorneyClientDisclaimer, ... }
export const ATTORNEYS = { 'scott-palmer': { name, role, isAttorney, barAdmissions: [{ state, year, status }] } }
export function formatBarStatus(): string  // produces "Maryland (2009); New Jersey admission pending"
```

From src/components/seo/LegalServiceSchema.astro (the exact JSON-LD pattern to follow):
```astro
---
import { FIRM, ATTORNEYS } from '../../lib/constants';
// build schema object
const schema = { '@context': 'https://schema.org', '@type': '...', ... };
---
<script type="application/ld+json" set:html={JSON.stringify(schema)} />
```

From src/layouts/MarketingLayout.astro (the thin pass-through pattern):
```astro
---
import BaseLayout from './BaseLayout.astro';
interface Props {
  title: string; description: string; ogImage?: string; ogType?: 'website'|'article'|'profile'; noindex?: boolean; emitOrgSchema?: boolean; emitWebSiteSchema?: boolean;
}
const props = Astro.props;
---
<BaseLayout {...props}><slot /></BaseLayout>
```

From src/components/layout/Header.astro (current nav — executor must read before modifying):
- Desktop nav has: Practice Areas (static link to /practice-areas), Scott Palmer, Pricing, Book a Fit Call
- Mobile nav has same links
- Vanilla JS script handles hamburger toggle with null-guard pattern:
  const hamburger = document.getElementById('hamburger') as HTMLButtonElement | null;
  if (hamburger && mobileNav) { ... }

From src/components/sections/CTASection.astro:
- No props — self-contained reusable section
- Renders dark (bg-ink) CTA block with "Book a Fit Call" → /contact
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coaching collection to content.config.ts + create 4 schema components + DisclaimerCallout</name>
  <files>
    content.config.ts,
    src/components/seo/FAQSchema.astro,
    src/components/seo/BreadcrumbSchema.astro,
    src/components/seo/PersonSchema.astro,
    src/components/seo/ServiceSchema.astro,
    src/components/legal/DisclaimerCallout.astro
  </files>
  <read_first>
    - content.config.ts (read entire file — understand existing collections before adding coaching)
    - src/components/seo/LegalServiceSchema.astro (the exact set:html pattern to replicate)
    - src/components/legal/FooterDisclaimer.astro (the FIRM constant import pattern for DisclaimerCallout)
    - src/lib/constants.ts (FIRM.noAttorneyClientDisclaimer, FIRM.url, ATTORNEYS['scott-palmer'].barAdmissions)
  </read_first>
  <action>
    **A. content.config.ts — add coaching collection**

    Insert the coaching collection definition BEFORE the final `export const collections` line.
    The new coaching collection block (insert after the `team` collection):

    ```ts
    const coaching = defineCollection({
      loader: glob({ pattern: '**/*.{md,mdx}', base: './src/content/coaching' }),
      schema: z.object({
        title: z.string(),
        description: z.string().max(160),
        audience: z.string(),
      }),
    });
    ```

    Update the export line from:
    ```ts
    export const collections = { blog, practiceAreas, team };
    ```
    to:
    ```ts
    export const collections = { blog, practiceAreas, team, coaching };
    ```

    Also create the directory src/content/coaching/ with a .gitkeep placeholder so the glob loader does not error on an empty directory:
    Run: mkdir -p src/content/coaching && touch src/content/coaching/.gitkeep

    **B. src/components/seo/FAQSchema.astro**

    Create this file with the exact pattern from LegalServiceSchema.astro (set:html):

    ```astro
    ---
    interface Props {
      faqs: Array<{ q: string; a: string }>;
    }
    const { faqs } = Astro.props;
    const schema = {
      '@context': 'https://schema.org',
      '@type': 'FAQPage',
      mainEntity: faqs.map(({ q, a }) => ({
        '@type': 'Question',
        name: q,
        acceptedAnswer: { '@type': 'Answer', text: a },
      })),
    };
    ---
    <script type="application/ld+json" set:html={JSON.stringify(schema)} />
    ```

    **C. src/components/seo/BreadcrumbSchema.astro**

    ```astro
    ---
    import { FIRM } from '../../lib/constants';
    interface Props {
      items: Array<{ name: string; url: string }>;
    }
    const { items } = Astro.props;
    const schema = {
      '@context': 'https://schema.org',
      '@type': 'BreadcrumbList',
      itemListElement: items.map((item, i) => ({
        '@type': 'ListItem',
        position: i + 1,
        name: item.name,
        item: `${FIRM.url}${item.url}`,
      })),
    };
    ---
    <script type="application/ld+json" set:html={JSON.stringify(schema)} />
    ```

    **D. src/components/seo/PersonSchema.astro**

    Uses Person (NOT the deprecated Attorney type per D-23). Reads all firm data from constants — never hardcoded strings:

    ```astro
    ---
    import { FIRM, ATTORNEYS } from '../../lib/constants';
    const scott = ATTORNEYS['scott-palmer'];
    const schema = {
      '@context': 'https://schema.org',
      '@type': 'Person',
      name: scott.name,
      jobTitle: scott.role,
      url: `${FIRM.url}/attorneys/scott-palmer`,
      worksFor: {
        '@type': 'LegalService',
        name: FIRM.legalName,
        url: FIRM.url,
      },
      alumniOf: [
        { '@type': 'EducationalOrganization', name: 'University of Baltimore School of Law' },
        { '@type': 'EducationalOrganization', name: 'University of Maryland, College Park' },
      ],
      hasCredential: scott.barAdmissions
        .filter((b) => b.status === 'active')
        .map((b) => ({
          '@type': 'EducationalOccupationalCredential',
          credentialCategory: 'Bar Admission',
          recognizedBy: { '@type': 'GovernmentOrganization', name: `${b.state} State Bar` },
        })),
    };
    ---
    <script type="application/ld+json" set:html={JSON.stringify(schema)} />
    ```

    **E. src/components/seo/ServiceSchema.astro**

    Emits Service type for individual practice area pages per PRAC-05:

    ```astro
    ---
    import { FIRM } from '../../lib/constants';
    interface Props {
      name: string;
      description: string;
    }
    const { name, description } = Astro.props;
    const schema = {
      '@context': 'https://schema.org',
      '@type': 'Service',
      name,
      description,
      provider: {
        '@type': 'LegalService',
        name: FIRM.legalName,
        url: FIRM.url,
      },
      areaServed: {
        '@type': FIRM.areaServed.type,
        name: FIRM.areaServed.name,
      },
    };
    ---
    <script type="application/ld+json" set:html={JSON.stringify(schema)} />
    ```

    **F. src/components/legal/DisclaimerCallout.astro**

    Page-level attorney-client disclaimer callout (distinct from footer disclaimer). Uses FIRM.noAttorneyClientDisclaimer from constants — never hardcoded text:

    ```astro
    ---
    import { FIRM } from '../../lib/constants';
    ---
    <div
      class="border-l-4 border-brand-blue-500 bg-surface-muted px-6 py-4 text-sm text-ink-muted leading-relaxed rounded-r-lg"
      role="note"
      aria-label="Legal disclaimer"
    >
      <strong class="text-ink">Note:</strong> {FIRM.noAttorneyClientDisclaimer}
    </div>
    ```

    After creating all files, run:
    npm run banned-words
    (Verify no banned vocabulary was accidentally included in any component.)
  </action>
  <verify>
    <automated>test -f content.config.ts && grep -c "coaching" content.config.ts && test -f src/components/seo/FAQSchema.astro && test -f src/components/seo/BreadcrumbSchema.astro && test -f src/components/seo/PersonSchema.astro && test -f src/components/seo/ServiceSchema.astro && test -f src/components/legal/DisclaimerCallout.astro</automated>
  </verify>
  <acceptance_criteria>
    - content.config.ts contains the string "coaching" at least twice (collection definition + export)
    - content.config.ts export line reads: export const collections = { blog, practiceAreas, team, coaching };
    - src/content/coaching/ directory exists (mkdir created it)
    - FAQSchema.astro contains: '@type': 'FAQPage'
    - FAQSchema.astro contains: set:html={JSON.stringify(schema)}
    - BreadcrumbSchema.astro contains: '@type': 'BreadcrumbList'
    - BreadcrumbSchema.astro contains: FIRM.url (imports from constants, not hardcoded)
    - PersonSchema.astro contains: '@type': 'Person' (NOT 'Attorney')
    - PersonSchema.astro contains: worksFor
    - PersonSchema.astro contains: alumniOf
    - PersonSchema.astro contains: hasCredential
    - ServiceSchema.astro contains: '@type': 'Service'
    - DisclaimerCallout.astro contains: FIRM.noAttorneyClientDisclaimer
    - DisclaimerCallout.astro contains: role="note"
    - npm run banned-words exits 0
  </acceptance_criteria>
  <done>All 6 infrastructure files created; coaching collection registered; npm run banned-words passes</done>
</task>

<task type="auto">
  <name>Task 2: Create PracticeAreaLayout.astro + update Header.astro with Practice Areas dropdown and Coaching nav link</name>
  <files>
    src/layouts/PracticeAreaLayout.astro,
    src/components/layout/Header.astro
  </files>
  <read_first>
    - src/layouts/MarketingLayout.astro (thin pass-through pattern — PracticeAreaLayout wraps this)
    - src/components/layout/Header.astro (FULL current file — understand exact HTML structure and script before modifying)
    - src/components/sections/CTASection.astro (no-props component reused by PracticeAreaLayout)
    - src/components/legal/DisclaimerCallout.astro (just created in Task 1 — import this)
    - src/components/seo/FAQSchema.astro (just created — import this)
    - src/components/seo/BreadcrumbSchema.astro (just created — import this)
    - src/components/seo/ServiceSchema.astro (just created — import this)
  </read_first>
  <action>
    **A. src/layouts/PracticeAreaLayout.astro**

    This layout wraps MarketingLayout and auto-injects all Phase 3 required pieces. Page authors who write MDX content files never wire these manually per D-12.

    ```astro
    ---
    import MarketingLayout from './MarketingLayout.astro';
    import CTASection from '../components/sections/CTASection.astro';
    import DisclaimerCallout from '../components/legal/DisclaimerCallout.astro';
    import FAQSchema from '../components/seo/FAQSchema.astro';
    import BreadcrumbSchema from '../components/seo/BreadcrumbSchema.astro';
    import ServiceSchema from '../components/seo/ServiceSchema.astro';

    interface Props {
      title: string;
      description: string;
      faqs: Array<{ q: string; a: string }>;
      slug: string;
      serviceDescription?: string;
    }
    const { title, description, faqs, slug, serviceDescription } = Astro.props;

    const breadcrumbs = [
      { name: 'Home', url: '/' },
      { name: title, url: `/practice-areas/${slug}` },
    ];
    ---
    <MarketingLayout {title} {description}>
      <FAQSchema {faqs} />
      <BreadcrumbSchema items={breadcrumbs} />
      <ServiceSchema name={title} description={serviceDescription ?? description} />
      <article class="mx-auto max-w-[1200px] px-md md:px-lg lg:px-xl py-12 md:py-16">
        <DisclaimerCallout />
        <div class="prose prose-lg max-w-prose mx-auto mt-8">
          <slot />
        </div>
        {faqs.length > 0 && (
          <section aria-labelledby="faq-heading" class="mt-16 max-w-prose mx-auto">
            <h2 id="faq-heading" class="font-display font-semibold text-2xl md:text-[28px] text-ink leading-[1.25]">
              Frequently Asked Questions
            </h2>
            <dl class="mt-8 space-y-8">
              {faqs.map(({ q, a }) => (
                <div>
                  <dt class="font-semibold text-ink">{q}</dt>
                  <dd class="mt-2 text-base leading-relaxed text-ink-muted">{a}</dd>
                </div>
              ))}
            </dl>
          </section>
        )}
      </article>
      <CTASection />
    </MarketingLayout>
    ```

    **B. src/components/layout/Header.astro — full replacement**

    Read the entire current Header.astro before writing. The new version keeps all existing structure and extends it with:
    1. Desktop nav: Replace static "Practice Areas" `<a>` link with a dropdown `<button>` + `<ul>` panel containing Corporate Law and Fractional GC links
    2. Desktop nav: Add "Coaching" link between the Practice Areas dropdown and "Scott Palmer"
    3. Nav order per D-06: Practice Areas dropdown | Coaching | Scott Palmer | Pricing | Book a Fit Call
    4. Mobile nav: Replace static "Practice Areas" link with a disclosure group (details/summary or inline list) listing Corporate Law and Fractional GC; add Coaching link
    5. Vanilla JS script: Extend existing hamburger script with dropdown logic using the same null-guard pattern

    Write the complete new Header.astro:

    ```astro
    ---
    import { FIRM } from '../../lib/constants';
    ---
    <header class="sticky top-0 z-50 bg-surface border-b border-border shadow-sm">
      <div class="mx-auto max-w-[1200px] px-md md:px-lg lg:px-xl h-[64px] md:h-[80px] flex items-center justify-between">

        <!-- Logo / Wordmark -->
        <a
          href="/"
          aria-label={`${FIRM.shortName} — Home`}
          class="font-display font-semibold text-[20px] md:text-[24px] text-ink hover:text-brand-blue-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500 transition-colors"
        >
          {FIRM.shortName}
        </a>

        <!-- Desktop nav + CTA (hidden on mobile) -->
        <nav aria-label="Main navigation" class="hidden md:flex items-center gap-8">

          <!-- Practice Areas dropdown -->
          <div class="relative">
            <button
              id="practice-btn"
              type="button"
              aria-haspopup="true"
              aria-expanded="false"
              aria-controls="practice-dropdown"
              class="text-base text-ink hover:text-brand-blue-500 flex items-center gap-1 transition-colors focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500"
            >
              Practice Areas
              <svg width="12" height="8" viewBox="0 0 12 8" aria-hidden="true" focusable="false">
                <path d="M1 1l5 5 5-5" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round"/>
              </svg>
            </button>
            <ul
              id="practice-dropdown"
              hidden
              class="absolute top-full left-0 mt-1 w-56 bg-surface border border-border rounded-lg shadow-md py-2 z-50"
            >
              <li>
                <a href="/practice-areas/corporate-law" class="block px-4 py-2 text-base text-ink hover:text-brand-blue-500 hover:bg-surface-muted transition-colors">
                  Corporate Law
                </a>
              </li>
              <li>
                <a href="/practice-areas/fractional-general-counsel" class="block px-4 py-2 text-base text-ink hover:text-brand-blue-500 hover:bg-surface-muted transition-colors">
                  Fractional GC
                </a>
              </li>
            </ul>
          </div>

          <a href="/coaching" class="text-base text-ink hover:text-brand-blue-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500 transition-colors">Coaching</a>
          <a href="/attorneys/scott-palmer" class="text-base text-ink hover:text-brand-blue-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500 transition-colors">Scott Palmer</a>
          <a href="/pricing" class="text-base text-ink hover:text-brand-blue-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500 transition-colors">Pricing</a>
          <a
            href="/contact"
            class="bg-brand-blue-500 hover:bg-brand-blue-600 text-white text-base font-semibold py-3 px-6 rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-brand-blue-500 focus:ring-offset-2"
          >
            Book a Fit Call
          </a>
        </nav>

        <!-- Mobile hamburger button (hidden on desktop) -->
        <button
          id="hamburger"
          type="button"
          aria-controls="mobile-nav"
          aria-expanded="false"
          aria-label="Open navigation menu"
          class="md:hidden p-3 text-ink hover:text-ink-muted focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500"
        >
          <svg width="20" height="14" viewBox="0 0 20 14" fill="none" aria-hidden="true" focusable="false">
            <rect y="0"  width="20" height="2" rx="1" fill="currentColor"/>
            <rect y="6"  width="20" height="2" rx="1" fill="currentColor"/>
            <rect y="12" width="20" height="2" rx="1" fill="currentColor"/>
          </svg>
        </button>
      </div>

      <!-- Mobile nav panel -->
      <div
        id="mobile-nav"
        class="hidden md:hidden bg-surface border-b border-border"
        role="navigation"
        aria-label="Mobile navigation"
      >
        <div class="mx-auto max-w-[1200px] px-md py-6 flex flex-col gap-6">
          <!-- Practice Areas disclosure group -->
          <details class="group">
            <summary class="text-base text-ink hover:text-brand-blue-500 py-[12px] cursor-pointer list-none flex items-center justify-between focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500">
              Practice Areas
              <svg width="12" height="8" viewBox="0 0 12 8" aria-hidden="true" class="transition-transform group-open:rotate-180">
                <path d="M1 1l5 5 5-5" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round"/>
              </svg>
            </summary>
            <div class="pl-4 flex flex-col gap-3 mt-2">
              <a href="/practice-areas/corporate-law" class="text-base text-ink hover:text-brand-blue-500 py-1 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500">Corporate Law</a>
              <a href="/practice-areas/fractional-general-counsel" class="text-base text-ink hover:text-brand-blue-500 py-1 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500">Fractional GC</a>
            </div>
          </details>
          <a href="/coaching" class="text-base text-ink hover:text-brand-blue-500 py-[12px] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500">Coaching</a>
          <a href="/attorneys/scott-palmer" class="text-base text-ink hover:text-brand-blue-500 py-[12px] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500">Scott Palmer</a>
          <a href="/pricing" class="text-base text-ink hover:text-brand-blue-500 py-[12px] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500">Pricing</a>
          <a
            href="/contact"
            class="block w-full text-center bg-brand-blue-500 hover:bg-brand-blue-600 text-white text-base font-semibold py-3 px-6 rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-brand-blue-500 focus:ring-offset-2"
          >
            Book a Fit Call
          </a>
        </div>
      </div>
    </header>

    <script>
      // Hamburger toggle (unchanged from Phase 2)
      const hamburger = document.getElementById('hamburger') as HTMLButtonElement | null;
      const mobileNav = document.getElementById('mobile-nav') as HTMLElement | null;

      if (hamburger && mobileNav) {
        hamburger.addEventListener('click', () => {
          const isOpen = hamburger.getAttribute('aria-expanded') === 'true';
          hamburger.setAttribute('aria-expanded', String(!isOpen));
          hamburger.setAttribute(
            'aria-label',
            isOpen ? 'Open navigation menu' : 'Close navigation menu'
          );
          mobileNav.classList.toggle('hidden');
        });

        document.addEventListener('keydown', (e) => {
          if (e.key === 'Escape' && hamburger.getAttribute('aria-expanded') === 'true') {
            hamburger.setAttribute('aria-expanded', 'false');
            hamburger.setAttribute('aria-label', 'Open navigation menu');
            mobileNav.classList.add('hidden');
            hamburger.focus();
          }
        });
      }

      // Practice Areas desktop dropdown
      const practiceBtn = document.getElementById('practice-btn') as HTMLButtonElement | null;
      const practiceDropdown = document.getElementById('practice-dropdown') as HTMLElement | null;

      if (practiceBtn && practiceDropdown) {
        practiceBtn.addEventListener('click', () => {
          const isOpen = practiceBtn.getAttribute('aria-expanded') === 'true';
          practiceBtn.setAttribute('aria-expanded', String(!isOpen));
          practiceDropdown.hidden = isOpen;
        });

        // Close on outside click
        document.addEventListener('click', (e) => {
          if (
            !practiceBtn.contains(e.target as Node) &&
            !practiceDropdown.contains(e.target as Node)
          ) {
            practiceBtn.setAttribute('aria-expanded', 'false');
            practiceDropdown.hidden = true;
          }
        });

        // Close on Escape
        document.addEventListener('keydown', (e) => {
          if (e.key === 'Escape' && practiceBtn.getAttribute('aria-expanded') === 'true') {
            practiceBtn.setAttribute('aria-expanded', 'false');
            practiceDropdown.hidden = true;
            practiceBtn.focus();
          }
        });
      }
    </script>
    ```

    After both files are written, run npm run banned-words to confirm no banned vocabulary.
    Then run npm run build to verify the entire site still builds with 0 errors.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/PracticeAreaLayout.astro exists
    - PracticeAreaLayout.astro contains: import CTASection
    - PracticeAreaLayout.astro contains: import DisclaimerCallout
    - PracticeAreaLayout.astro contains: import FAQSchema
    - PracticeAreaLayout.astro contains: import BreadcrumbSchema
    - PracticeAreaLayout.astro contains: import ServiceSchema
    - PracticeAreaLayout.astro contains: interface Props (with title, description, faqs, slug)
    - PracticeAreaLayout.astro contains: slot (for MDX content)
    - PracticeAreaLayout.astro contains: CTASection (auto-injected)
    - PracticeAreaLayout.astro contains: DisclaimerCallout (auto-injected)
    - Header.astro contains: aria-haspopup="true"
    - Header.astro contains: aria-expanded="false"
    - Header.astro contains: id="practice-dropdown"
    - Header.astro contains: /practice-areas/corporate-law
    - Header.astro contains: /practice-areas/fractional-general-counsel
    - Header.astro contains: href="/coaching"
    - Header.astro desktop nav order: Practice Areas button BEFORE Coaching BEFORE Scott Palmer BEFORE Pricing BEFORE Book a Fit Call
    - Header.astro script contains: practice-btn (dropdown open/close logic)
    - Header.astro script contains: Escape (closes dropdown)
    - npm run build exits 0
    - npm run banned-words exits 0
  </acceptance_criteria>
  <done>PracticeAreaLayout exists and auto-injects all required pieces. Header has Practice Areas dropdown and Coaching link in correct nav order. npm run build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX frontmatter → schema components | FAQs and descriptions from MDX are passed as props to FAQSchema and ServiceSchema; no user-supplied input at runtime (static SSG) |
| Header dropdown → navigation | Click/keyboard events handled by vanilla JS; no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Spoofing | Header dropdown JS | accept | Static site — no auth context; dropdown manages only nav state |
| T-03-01-02 | Information Disclosure | PersonSchema.astro | accept | Only public data (name, role, public URL, education) — no PII beyond what's intentionally published |
| T-03-01-03 | Tampering | content.config.ts coaching collection | accept | Build-time only; Zod validates all MDX frontmatter at build; invalid files fail the build |
</threat_model>

<verification>
```bash
# Confirm coaching registered in content.config.ts
grep "coaching" content.config.ts

# Confirm all schema components exist
ls src/components/seo/FAQSchema.astro src/components/seo/BreadcrumbSchema.astro src/components/seo/PersonSchema.astro src/components/seo/ServiceSchema.astro

# Confirm DisclaimerCallout exists
ls src/components/legal/DisclaimerCallout.astro

# Confirm PracticeAreaLayout exists
ls src/layouts/PracticeAreaLayout.astro

# Confirm Header dropdown links
grep "practice-areas/corporate-law" src/components/layout/Header.astro
grep "practice-areas/fractional-general-counsel" src/components/layout/Header.astro
grep "/coaching" src/components/layout/Header.astro

# Full build check
npm run build
```
</verification>

<success_criteria>
- coaching collection defined in content.config.ts and included in export
- src/content/coaching/ directory exists
- FAQSchema, BreadcrumbSchema, PersonSchema, ServiceSchema components exist and follow set:html pattern
- DisclaimerCallout exists and uses FIRM.noAttorneyClientDisclaimer from constants
- PracticeAreaLayout wraps MarketingLayout and auto-injects: DisclaimerCallout, FAQSchema, BreadcrumbSchema, ServiceSchema, slot, CTASection
- Header desktop nav: Practice Areas dropdown (Corporate Law, Fractional GC) | Coaching | Scott Palmer | Pricing | Book a Fit Call
- Header dropdown is ARIA-accessible: aria-haspopup, aria-expanded, aria-controls on trigger; hidden attribute toggled; Escape closes
- npm run build exits 0
- npm run banned-words exits 0
</success_criteria>

<output>
After completion, create .planning/phases/03-firm-substance-people-practice-areas-pricing/03-01-SUMMARY.md
</output>
