---
phase: 01-foundation-live-skeleton
plan: 05
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - .planning/phases/01-foundation-live-skeleton/01-05-DEPLOY-NOTES.md
autonomous: false
requirements:
  - FOUND-01
user_setup:
  - service: github
    why: "First push to main triggers Vercel auto-deploy and runs CI workflow"
    env_vars: []
    dashboard_config:
      - task: "Confirm the local git repo's `origin` remote points to https://github.com/spalmer47/scopal.git and Scott has push access"
        location: "GitHub.com → spalmer47/scopal → Settings"
  - service: vercel
    why: "Hosting platform — auto-deploys from GitHub, applies vercel.json headers, manages TLS"
    env_vars: []
    dashboard_config:
      - task: "Sign in at vercel.com (Scott's existing account); import the spalmer47/scopal repo as a new project; framework auto-detect should select Astro; do NOT set any environment variables (Phase 1 needs none); deploy"
        location: "vercel.com/dashboard → Add New → Project → Import Git Repository"
      - task: "Add custom domain `scopalfirm.com` and `www.scopalfirm.com` in the Vercel project's Domains tab BEFORE updating registrar DNS (this lets Vercel pre-provision the TLS cert)"
        location: "Vercel project → Settings → Domains"
      - task: "At the domain registrar (where scopalfirm.com is registered), update DNS A record for apex to 76.76.21.21 (Vercel) AND add CNAME for www → cname.vercel-dns.com — exact values displayed in the Vercel Domains tab"
        location: "Domain registrar control panel"
must_haves:
  truths:
    - "`https://scopalfirm.com/` returns HTTP 200 with Content-Type: text/html and the body contains the literal text 'Scopal Firm'"
    - "`curl -I https://scopalfirm.com/` returns all six security headers: Content-Security-Policy, X-Frame-Options: DENY, X-Content-Type-Options: nosniff, Referrer-Policy: strict-origin-when-cross-origin, Permissions-Policy, Strict-Transport-Security: max-age=63072000; includeSubDomains; preload"
    - "Pushing a new commit to `main` triggers a Vercel deployment that completes within ~60 seconds and updates the live URL"
    - "Opening a pull request to `main` triggers the GitHub Actions CI workflow and reports back to the PR all five gate results (banned-words, build, linkcheck, a11y, Lighthouse, gitleaks)"
    - "View-source on `https://scopalfirm.com/` shows footer NAP + attorney advertising line + UPL disclaimer + no-attorney-client disclaimer + JSON-LD LegalService block"
    - "`https://scopalfirm.com/sitemap-index.xml` (or `/sitemap-0.xml`) returns 200 with valid XML referencing `https://scopalfirm.com/`"
    - "`https://scopalfirm.com/robots.txt` returns 200 and contains the literal lines 'Disallow: /api/' and 'Sitemap: https://scopalfirm.com/sitemap-index.xml'"
  artifacts:
    - path: ".planning/phases/01-foundation-live-skeleton/01-05-DEPLOY-NOTES.md"
      provides: "Deployment audit trail — Vercel project ID, deployment URL, header verification output, smoke test results, follow-up TODOs (HSTS preload submission)"
  key_links:
    - from: "GitHub repo spalmer47/scopal main branch"
      to: "Vercel project (auto-deploy webhook)"
      via: "Vercel ↔ GitHub Git integration"
      pattern: "deployment success"
    - from: "Vercel project domain settings"
      to: "scopalfirm.com DNS A/CNAME records at registrar"
      via: "Vercel-supplied A=76.76.21.21 and CNAME=cname.vercel-dns.com values"
      pattern: "DNS resolution"
    - from: "vercel.json"
      to: "every HTTP response from the live site"
      via: "Vercel edge applies headers configuration"
      pattern: "all six security headers visible in curl -I"
---

<objective>
Take the green-CI, building-locally project and put it LIVE at `https://scopalfirm.com` with auto-deploy from `main`. After this plan, the Walking Skeleton is COMPLETE — a real visitor reaching the production URL gets a static Astro page through the Vercel edge CDN with all six security headers, all three required disclaimers, JSON-LD LegalService schema, and the CI merge gate enforcing future contributions.

Purpose: This is the Walking Skeleton's payoff slice. The previous four plans assembled the parts; this plan proves the parts work together end-to-end on real infrastructure. It is intentionally small (one git push, two checkpoints, a verification script) because the hard work was front-loaded. Most steps are checkpoint-gated because they require Scott's GitHub/Vercel/DNS credentials.

Output: A live URL, plus `01-05-DEPLOY-NOTES.md` capturing the Vercel project ID, the production deployment URL, the curl header verification output, and the post-launch follow-up TODO (HSTS preload list submission once the site is stable for a week).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-live-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-live-skeleton/01-SKELETON.md
@.claude/CLAUDE.md

<interfaces>
This plan does NOT create new code. It exercises the artifacts from Plans 01–04:
- `vercel.json` (Plan 03) → applied by Vercel at the edge for every response
- `astro.config.mjs` `site: 'https://scopalfirm.com'` (Plan 01) → drives sitemap canonical URLs
- `.github/workflows/ci.yml` (Plan 04) → activates on the first push to main
- `dist/index.html` (Plans 02 + 03) → the artifact Vercel serves

The user (Scott) is a non-developer attorney. EVERY command and every dashboard click MUST be explained in one sentence of plain English BEFORE the action (per CLAUDE.md "How to Communicate"). For dashboard steps, describe what Scott will see at each click.

The two checkpoints in this plan are:
1. `vercel-project-link` — Scott logs into Vercel, imports the GitHub repo, deploys, confirms the Vercel-issued preview URL works. Claude cannot do this without Scott's Vercel credentials.
2. `dns-cutover` — Scott updates DNS at his registrar so `scopalfirm.com` points to Vercel. Claude cannot access the registrar.

Both checkpoints have Claude do all the verification work AFTER Scott completes the manual step.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initial commit + push to GitHub main, triggering CI and Vercel preview build prerequisites</name>
  <files>(no source files modified — this task commits and pushes the work from Plans 01–04)</files>
  <read_first>
    - .planning/phases/01-foundation-live-skeleton/01-01-SUMMARY.md (if exists — confirms scaffold completed)
    - .planning/phases/01-foundation-live-skeleton/01-02-SUMMARY.md (compliance core)
    - .planning/phases/01-foundation-live-skeleton/01-03-SUMMARY.md (security/content/env)
    - .planning/phases/01-foundation-live-skeleton/01-04-SUMMARY.md (CI rails)
    - .gitignore (Plan 03 — confirms `.env*` patterns are excluded BEFORE we run `git add`)
    - .claude/CLAUDE.md (How to Communicate — explain commands in plain English first)
  </read_first>
  <action>
    Step 1 — pre-flight integrity audit. Before adding anything to git, confirm:

    ```
    # No .env files (other than .env.example) exist in the working tree
    find . -maxdepth 2 -name ".env*" -not -name ".env.example" -not -path "./node_modules/*"
    # → must print nothing

    # gitignore actually ignores .env
    git check-ignore -q .env && echo "OK: .env gitignored"

    # Build still works
    npm run build

    # Banned-words clean
    npm run banned-words

    # Local link check on dist
    npm run linkcheck

    # Local axe check (this starts a preview server, runs axe, kills the server)
    npm run a11y
    ```

    All five must succeed. If any fails, STOP — go back to the relevant plan. Do not push broken code.

    Step 2 — explain to Scott what we are about to do, then check git remote:

    Plain-English script (read this verbatim to user before running):
    "I'm about to send the new website code to GitHub. GitHub is like Google Drive for code — once it's there, our hosting service Vercel will automatically pick it up and put a live preview on the internet. First I'm going to make sure the code is connected to your repo at https://github.com/spalmer47/scopal."

    ```
    git remote -v
    ```

    Expected output: `origin  https://github.com/spalmer47/scopal.git (fetch)` and `(push)`.

    If `origin` is missing or wrong, set it (Scott confirms the URL is right):
    ```
    git remote remove origin 2>/dev/null
    git remote add origin https://github.com/spalmer47/scopal.git
    ```

    Step 3 — stage files. Use explicit paths — DO NOT use `git add .` or `git add -A` (could pick up an accidentally-created `.env` if `.gitignore` had a typo). Stage only what each prior plan was supposed to create:

    ```
    git add \
      .gitignore .env.example \
      package.json package-lock.json README.md \
      astro.config.mjs tsconfig.json \
      vercel.json content.config.ts \
      .lighthouserc.json \
      public/ src/ scripts/ \
      .github/ \
      .planning/phases/01-foundation-live-skeleton/
    ```

    The .planning/ paths capture the SKELETON.md, the five PLAN.md files, and the four SUMMARY files for traceability.

    Step 4 — confirm staging is clean. Look for any unexpected files:

    ```
    git status --porcelain
    ```

    Review the output with Scott. Anything beginning with `??` (untracked) that looks unexpected (e.g., `.env`, `dist/`, `node_modules/` — all of which should be gitignored) means the gitignore failed. Stop and investigate. If only `.planning/` extras and project files appear, proceed.

    Step 5 — commit. Use a structured commit message:

    ```
    git commit -m "$(cat <<'EOF'
    feat(01-foundation): walking skeleton — Astro 6 + Tailwind v4 + Vercel + CI

    Lands the foundation phase: Astro 6 project scaffolded with Tailwind v4 (CSS-first
    @theme config), MDX, sitemap, Vercel adapter; BaseLayout + Footer enforce the firm's
    NAP and three legal disclaimers (attorney advertising, UPL, no attorney-client) on
    every page automatically; LegalService JSON-LD on /; vercel.json ships all six
    required security headers including HSTS preload; .env files gitignored with
    .env.example documenting required vars; content collections defined (empty) for
    blog/practiceAreas/team; GitHub Actions CI runs banned-words, axe-core, linkinator,
    gitleaks, Lighthouse on every PR. Closes FOUND-01..07.

    Refs: .planning/phases/01-foundation-live-skeleton/
    EOF
    )"
    ```

    Step 6 — push. Explain first:
    "Now I'll send everything to GitHub. After this command finishes, the CI checks will run automatically on GitHub's servers — I'll watch them in the next step."

    ```
    git push -u origin main
    ```

    If push is rejected (existing remote main has commits), STOP and consult Scott — do NOT force-push without explicit approval.

    Step 7 — wait for CI. After push:
    "GitHub is now running our five quality checks. I'll wait about a minute and then look at the results. If anything fails, we'll fix it before going to Vercel."

    Wait ~90 seconds, then check the workflow:
    ```
    gh run list --workflow=ci.yml --branch=main --limit=1 --json status,conclusion,databaseId,url
    ```

    If `gh` CLI is not installed or not authenticated, instead direct Scott to https://github.com/spalmer47/scopal/actions and ask him to confirm the run is green. Wait for `conclusion: success`. If `conclusion: failure`, fetch the failing job logs (`gh run view <id> --log-failed`) and fix the cause before proceeding to Task 2.
  </action>
  <verify>
    <automated>find . -maxdepth 2 -name ".env*" -not -name ".env.example" -not -path "./node_modules/*" -print -quit | grep -q . && echo "FAIL stray env" && exit 1 || echo "PASS no stray env" && git check-ignore -q .env && npm run build && npm run banned-words && git remote get-url origin | grep -q "github.com[/:]spalmer47/scopal" && git log -1 --pretty=%s | grep -q "01-foundation" && git ls-remote origin main | grep -q "$(git rev-parse HEAD)"</automated>
  </verify>
  <acceptance_criteria>
    - `find . -maxdepth 2 -name ".env*" -not -name ".env.example" -not -path "./node_modules/*"` returns no output (no stray env files)
    - `git check-ignore -q .env` exits 0
    - `npm run build` exits 0 immediately before commit
    - `npm run banned-words` exits 0 immediately before commit
    - `git remote get-url origin` matches `https://github.com/spalmer47/scopal.git` (or `git@github.com:spalmer47/scopal.git`)
    - The latest commit message contains the substring `01-foundation`
    - `git ls-remote origin main` returns the same SHA as `git rev-parse HEAD` (push reached the remote)
    - GitHub Actions CI run for the pushed commit reports `conclusion: success` (verified via `gh run list` or by Scott confirming https://github.com/spalmer47/scopal/actions shows a green check)
    - No commit in the pushed history matches gitleaks patterns (the gitleaks step in CI must pass)
  </acceptance_criteria>
  <done>
    Code is on GitHub `main`. CI ran and all five gates passed (banned-words, build, linkcheck, a11y, Lighthouse, gitleaks). Ready for Vercel project link.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (CHECKPOINT): Scott imports the GitHub repo into Vercel, deploys, and shares the preview URL</name>
  <what-built>Plans 01–04 plus Task 1 produced a green-CI Astro project on GitHub `main`. All artifacts to be served (`dist/index.html` with disclaimers + JSON-LD, `vercel.json` with security headers, `public/robots.txt`, the generated sitemap) build cleanly. The repo is ready for Vercel to import.</what-built>
  <how-to-verify>
    Claude has already done all preparation. Scott now needs to perform the Vercel-side steps. Claude reads each step out loud (plain English) and waits.

    Step A — sign into Vercel:
    "Open https://vercel.com in your browser and sign in with the account you want to host this site on. (If you've never used Vercel, click 'Sign Up' and use 'Continue with GitHub' — that links the two services together so Vercel can read your repos.)"

    Step B — import the project:
    "Click 'Add New...' in the top right, then 'Project'. Vercel will show a list of your GitHub repositories. Find 'spalmer47/scopal' and click 'Import'."

    Step C — configure (mostly accept defaults):
    "Vercel will detect this is an Astro project automatically. You'll see:
       - Framework Preset: Astro (correct — leave as-is)
       - Root Directory: ./ (correct — leave as-is)
       - Build Command: astro build (correct — leave as-is)
       - Output Directory: dist (correct — leave as-is)
       - Install Command: npm install (correct — leave as-is)
       - Environment Variables: LEAVE EMPTY (Phase 1 doesn't need any; Phase 4 will add them)
     Click 'Deploy' at the bottom."

    Step D — wait for the build to finish:
    "Vercel will run a build. This usually takes 30–60 seconds. When it's done, Vercel shows confetti and a screenshot of the page. The page will say 'Scopal Firm' as a heading. Click the 'Visit' button or copy the URL — it will look something like https://scopal-xxxx.vercel.app/. Share that URL with me."

    Step E — Claude verifies the preview:
    Once Scott provides the URL (call it `$PREVIEW_URL`), Claude runs:
    ```
    curl -sI "$PREVIEW_URL" | grep -E "^(HTTP|content-security-policy|x-frame-options|x-content-type-options|referrer-policy|permissions-policy|strict-transport-security):"
    ```
    Confirm the response is `HTTP/2 200` and all six headers appear. If any header is missing, debug `vercel.json` (most likely cause: file not at repo root, or JSON syntax error blocked Vercel from applying it).

    Then verify the body contents:
    ```
    curl -s "$PREVIEW_URL" | grep -F "Attorney Advertising. Prior results"
    curl -s "$PREVIEW_URL" | grep -F '"@type":"LegalService"'
    curl -s "$PREVIEW_URL" | grep -F "Maryland (2009); New Jersey admission pending"
    ```

    All three must succeed. If they do, Claude announces:
    "All six security headers are live. The footer disclaimers and JSON-LD are rendering correctly on the Vercel preview. We're ready to point your real domain at this site."

    What Scott approves at this checkpoint:
    1. The Vercel preview URL is accessible and shows the placeholder homepage.
    2. Claude's header verification output (Claude pastes the curl output) shows all six security headers.
    3. The body verification shows disclaimers and JSON-LD.
    4. Scott is ready to update DNS in the next checkpoint.
  </how-to-verify>
  <resume-signal>Reply with the Vercel preview URL and either "approved" (proceed to DNS cutover) OR a description of what looks wrong (Claude debugs)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (CHECKPOINT): Scott adds scopalfirm.com to Vercel and updates DNS at the registrar; Claude verifies live</name>
  <what-built>The project is live on a Vercel preview URL with all headers and content correct (Task 2 verified). Now we cut the real domain `scopalfirm.com` over to Vercel.</what-built>
  <how-to-verify>
    The order matters: ADD THE DOMAIN IN VERCEL FIRST (so the cert pre-provisions), THEN update DNS. This avoids the cert-warning window during cutover.

    Step A — add domain in Vercel (Scott):
    "In your Vercel project, click 'Settings' (in the project's top nav), then 'Domains' in the left sidebar. In the input box, type scopalfirm.com and press Enter. Vercel will say 'Invalid Configuration' next to the domain — that's expected because the DNS isn't pointing here yet. Click 'View Configuration' or look at the panel that appears: Vercel will tell you exactly what DNS records to add. Take a screenshot of those records and share them with me. Also add a second domain: www.scopalfirm.com (Vercel will offer to redirect it to the apex; accept that)."

    Step B — Claude reviews the DNS records Scott was given:
    Typical records Vercel asks for:
    - Apex (`scopalfirm.com`): A record → `76.76.21.21`
    - www (`www.scopalfirm.com`): CNAME → `cname.vercel-dns.com`

    The exact values are whatever Vercel shows Scott — do not assume; cross-check against the screenshot. Confirm with Scott:
    "Those are the records we need to add at the place where you registered scopalfirm.com (your domain registrar — possibly GoDaddy, Namecheap, Squarespace Domains, or Vercel itself if you registered through them). What registrar is scopalfirm.com at?"

    Step C — Scott updates DNS at the registrar:
    Plain-English explanation: "DNS is the system that translates a domain name into a server address. We're telling the internet that scopalfirm.com now points to Vercel's servers instead of wherever it pointed before."

    Once Scott reports DNS is updated, wait 2–10 minutes (DNS can take up to 24h but typically resolves in a few minutes for new records).

    Step D — Claude verifies propagation:
    ```
    # Resolve the apex
    dig +short scopalfirm.com A
    # → should return 76.76.21.21 (or whatever Vercel said)

    dig +short www.scopalfirm.com CNAME
    # → should return cname.vercel-dns.com.
    ```

    If those resolve correctly, return to the Vercel Domains tab — the "Invalid Configuration" warning should clear and a green check should appear. If after 10 minutes propagation isn't done, run `dig` against a public resolver explicitly: `dig @1.1.1.1 +short scopalfirm.com A`.

    Step E — Claude does the FINAL header + content verification on the real URL:
    ```
    # All six security headers
    curl -sI https://scopalfirm.com/ | grep -iE "^(HTTP|content-security-policy|x-frame-options|x-content-type-options|referrer-policy|permissions-policy|strict-transport-security):"

    # HSTS exact value (FOUND-02 / SEC-01)
    curl -sI https://scopalfirm.com/ | grep -i "^strict-transport-security:" | grep -F "max-age=63072000; includeSubDomains; preload"

    # 200 + body content
    curl -s -o /tmp/home.html -w "HTTP %{http_code}\n" https://scopalfirm.com/
    grep -F "Scopal Firm" /tmp/home.html
    grep -F "Attorney Advertising. Prior results" /tmp/home.html
    grep -F "Maryland (2009); New Jersey admission pending" /tmp/home.html
    grep -F "does not create an attorney-client relationship" /tmp/home.html
    grep -F "application/ld+json" /tmp/home.html
    grep -F "\"@type\":\"LegalService\"" /tmp/home.html
    grep -F "\"addressLocality\":\"Annandale\"" /tmp/home.html

    # Sitemap + robots
    curl -sI https://scopalfirm.com/sitemap-index.xml | head -1
    curl -sI https://scopalfirm.com/sitemap-0.xml | head -1
    curl -s https://scopalfirm.com/robots.txt | grep -F "Sitemap: https://scopalfirm.com/sitemap-index.xml"
    curl -s https://scopalfirm.com/robots.txt | grep -F "Disallow: /api/"
    ```

    If everything passes, Claude tells Scott:
    "scopalfirm.com is LIVE. All six security headers are in place. The footer disclaimers, JSON-LD, sitemap, and robots.txt are all working. Anytime you push code to the main branch on GitHub, the site updates automatically in about a minute. We're done with Phase 1 — ready to plan Phase 2 (the real homepage) whenever you are."

    Step F — Claude writes `01-05-DEPLOY-NOTES.md` capturing the verification output (see <output> section below) and creates the optional follow-up TODO:
    "Once the site has been stable for a week, we should submit https://scopalfirm.com to the HSTS preload list at https://hstspreload.org. That permanently bakes HTTPS-only into every browser. I'll add this to your DECISIONS.md as a follow-up so we don't forget."

    What Scott approves at this checkpoint:
    1. https://scopalfirm.com loads in his browser with no warnings (no cert error, no "Not Secure" indicator).
    2. The page shows the placeholder homepage with the H1 "Scopal Firm" and the footer.
    3. He's seen Claude's curl output confirming all six headers.
    4. He's ready to declare Phase 1 complete.
  </how-to-verify>
  <resume-signal>Reply with "approved — Phase 1 complete" (Claude writes deploy notes and declares done) OR describe any remaining issue</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local working tree → GitHub remote | Untrusted commits cross here; gitignore + gitleaks prevent secret leakage |
| GitHub `main` → Vercel auto-deploy webhook | Trusted CI gate must succeed before Vercel ships |
| DNS registrar → Vercel | DNS hijack would redirect users; mitigated by registrar credentials only Scott controls |
| Public visitor → live `scopalfirm.com` | First request after cutover is the binding test of all upstream work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-32 | Information Disclosure | Accidental `git add .` includes a `.env` file | mitigate | Task 1 uses explicit-path `git add` (never `-A` / `.`); pre-flight `find` for stray env files; `git check-ignore` verification gate before push |
| T-01-33 | Spoofing | Domain takeover during DNS cutover (cert-warning window) | mitigate | Task 3 adds the domain in Vercel BEFORE updating DNS — Vercel pre-provisions the Let's Encrypt cert so it's ready the moment DNS resolves (Pitfall 9) |
| T-01-34 | Tampering | Vercel build picks up stale cache after a future major upgrade | accept (Phase 1) | Documented as a follow-up in `01-05-DEPLOY-NOTES.md`: "After major Astro/Tailwind upgrades, Redeploy → Clear Build Cache once" (Pitfall 8) |
| T-01-35 | Spoofing | First user reaches site over HTTP (pre-HSTS-cache) | mitigate | `vercel.json` `upgrade-insecure-requests` plus Vercel's automatic HTTP→HTTPS redirect; HSTS header present on every successful HTTPS response from the first request; preload submission documented as a follow-up TODO once stability is proven |
| T-01-36 | Information Disclosure | Vercel build accidentally exposes a secret in `dist/` | accept (Phase 1) | Phase 1 reads no env vars (both schema entries optional); SEC-02 audit (`grep -ri 'sk_\|api_key' dist/`) is a Phase 4 gate. Task 1's pre-flight checks the local `dist/` similarly; Plan 04's CI step adds `gitleaks` for the source tree. |
| T-01-37 | Tampering | First PR after launch bypasses CI by force-push to main | accept (Phase 1) | GitHub branch protection on `main` is a follow-up best-practice; recommend in deploy notes. Solo-developer / non-malicious context lowers the practical risk. |
| T-01-38 | Repudiation | Lack of audit trail for the deployment | mitigate | `01-05-DEPLOY-NOTES.md` captures Vercel project ID, deployment URL, full curl header output, DNS records added, timestamp, and follow-up TODOs |
</threat_model>

<verification>
1. `git ls-remote origin main` returns the local HEAD SHA (push succeeded).
2. CI run on the pushed commit reports `conclusion: success` (all five gates green).
3. `curl -sI https://scopalfirm.com/` returns `HTTP/2 200` and all six security headers.
4. `curl -sI https://scopalfirm.com/ | grep -i strict-transport-security` returns the exact value `max-age=63072000; includeSubDomains; preload`.
5. `curl -s https://scopalfirm.com/` body contains all three disclaimers, the bar status string, and `"@type":"LegalService"`.
6. `curl -s https://scopalfirm.com/robots.txt` contains `Sitemap: https://scopalfirm.com/sitemap-index.xml` and `Disallow: /api/`.
7. `curl -sI https://scopalfirm.com/sitemap-index.xml` (or `/sitemap-0.xml`) returns 200.
8. Pushing a one-line trivial commit to `main` triggers a Vercel deploy that completes within 90 seconds (smoke test for auto-deploy).
</verification>

<success_criteria>
- `https://scopalfirm.com/` returns 200 with the placeholder homepage.
- All six security headers verified live with the exact values FOUND-02 specifies.
- All three disclaimers + LegalService JSON-LD present in production HTML.
- CI gate enforces five quality checks on every PR.
- Auto-deploy from `main` confirmed working.
- HSTS preload submission documented as a post-stability follow-up TODO.
- All seven FOUND-XX requirements satisfied: FOUND-01 (live + auto-deploy), FOUND-02 (headers verified), FOUND-03 (gitignore + .env.example + gitleaks running), FOUND-04 (constants single source), FOUND-05 (CI green), FOUND-06 (collections compile), FOUND-07 (BaseLayout enforces footer disclaimers).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-live-skeleton/01-05-DEPLOY-NOTES.md` recording:

- **Vercel project**: project ID, project URL (vercel.com/<team>/<project>), team/account name
- **Production deployment**: URL, deployment ID, build duration
- **DNS records added**: exact values Scott entered at the registrar (apex A record, www CNAME)
- **Header verification output**: full text of `curl -sI https://scopalfirm.com/` (paste the response head)
- **Body verification output**: results of the seven `grep -F` smoke checks (each ✓ or ✗ with the matching line)
- **Sitemap + robots verification**: HTTP status of `/sitemap-index.xml` (or `/sitemap-0.xml`) and `/robots.txt`
- **Auto-deploy smoke test**: timestamp of test commit to `main` + timestamp of resulting deployment success
- **Follow-up TODOs** (Claude also adds these to `.planning/DECISIONS.md` per CLAUDE.md):
    1. Submit `scopalfirm.com` to https://hstspreload.org once site is stable for ~7 days
    2. Enable GitHub branch protection on `main` (require PR + passing CI before merge)
    3. After any future major Astro / Tailwind upgrade: in Vercel Dashboard → Deployments → Redeploy → Clear Build Cache (Pitfall 8 / P2-6)
    4. Confirm Vercel Web Analytics requests are not being blocked by CSP (Assumption A1) by visiting the site and checking browser DevTools console — if blocked, update `script-src`/`connect-src` and redeploy
- **Phase 1 close-out**: explicit confirmation that all 7 FOUND-XX requirements are satisfied with paths to the artifacts that prove each
</output>
