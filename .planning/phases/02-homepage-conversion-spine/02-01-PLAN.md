---
phase: 02-homepage-conversion-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Header.astro
  - src/pages/contact.astro
autonomous: false
requirements:
  - HOME-05
must_haves:
  truths:
    - "src/components/layout/Header.astro contains 'aria-label=\"Main navigation\"'"
    - "src/components/layout/Header.astro contains 'Book a Fit Call'"
    - "src/components/layout/Header.astro contains 'Practice Areas'"
    - "src/components/layout/Header.astro contains 'Scott Palmer'"
    - "src/components/layout/Header.astro contains 'Pricing'"
    - "src/components/layout/Header.astro contains 'aria-expanded'"
    - "src/components/layout/Header.astro contains 'id=\"mobile-nav\"'"
    - "src/pages/contact.astro exists"
    - "src/pages/contact.astro contains 'Get in Touch'"
    - "src/pages/contact.astro contains 'Contact — Annandale, NJ | Scopal Firm'"
    - "npm run build exits 0 and dist/contact/index.html exists"
  artifacts:
    - path: "src/components/layout/Header.astro"
      provides: "Sticky header with nav links, CTA button, mobile hamburger"
      contains: "aria-label=\"Main navigation\""
    - path: "src/pages/contact.astro"
      provides: "Placeholder contact page"
      contains: "Get in Touch"
  key_links:
    - from: "Header.astro hamburger button"
      to: "#mobile-nav panel"
      via: "vanilla <script> toggles aria-expanded + hidden class"
      pattern: "aria-expanded"
    - from: "Header.astro CTA button"
      to: "/contact"
      via: "href=/contact"
      pattern: "/contact"
---

<objective>
Update the Phase 1 header with real navigation links, a "Book a Fit Call" CTA button, and a vanilla-JS hamburger menu for mobile. Then create the placeholder /contact page that all CTA buttons across the site will link to.

Purpose: Every CTA button on the site links to /contact. Without this plan running first, Plan 02 has a broken link destination. The header update also satisfies HOME-05 (persistent CTA at top of every page).

Output:
- Updated src/components/layout/Header.astro — sticky, nav links, CTA, mobile hamburger with aria
- New src/pages/contact.astro — placeholder page with mailto link, using MarketingLayout
</objective>

<execution_context>
@/Users/spalmer/Documents/Claude Code/Scopal Website/.claude/CLAUDE.md
</execution_context>

<context>
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/ROADMAP.md
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/phases/02-homepage-conversion-spine/02-CONTEXT.md
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/phases/02-homepage-conversion-spine/02-UI-SPEC.md

<interfaces>
<!-- Read before touching any file. Extracted from existing Phase 1 source. -->

From src/lib/constants.ts (FIRM export — all firm data reads from here):
```typescript
export const FIRM = {
  legalName: 'Scopal Firm, LLC',
  shortName: 'Scopal Firm',
  url: 'https://scopalfirm.com',
  email: 'scott@scopalfirm.com',
  phone: null as string | null,
  address: {
    addressLocality: 'Annandale',
    addressRegion: 'NJ',
    addressCountry: 'US',
  },
  ...
} as const;
```

From src/layouts/MarketingLayout.astro (thin wrapper — use for contact.astro):
```astro
---
import BaseLayout from './BaseLayout.astro';
interface Props {
  title: string;
  description: string;
  ogImage?: string;
  ogType?: 'website' | 'article' | 'profile';
  noindex?: boolean;
  emitOrgSchema?: boolean;
}
const props = Astro.props;
---
<BaseLayout {...props}><slot /></BaseLayout>
```

From src/components/layout/Header.astro (current Phase 1 state — full replacement):
```astro
---
import { FIRM } from '../../lib/constants';
---
<header class="bg-surface border-b border-border">
  <div class="mx-auto max-w-[1200px] px-md md:px-lg lg:px-xl h-[64px] md:h-[80px] flex items-center justify-between">
    <a href="/" aria-label={`${FIRM.shortName} — Home`} class="font-display font-semibold text-[20px] md:text-[24px] text-ink ...">
      {FIRM.shortName}
    </a>
    <a href={`mailto:${FIRM.email}`} class="hidden md:inline-flex ...">
      Schedule a Consultation
    </a>
  </div>
</header>
```

Color tokens available in global.css @theme (use these Tailwind class names directly):
- bg-surface, text-ink, text-ink-muted, border-border
- bg-brand-blue-500, hover:bg-brand-blue-600, text-brand-blue-500
- bg-surface-muted
- font-display (Fraunces), font-body (Inter)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Prerequisite: Commit logo files before execution</name>
  <what-built>Nothing yet — this step happens before any code is written.</what-built>
  <how-to-verify>
    Per CONTEXT.md D-08, logo files must be in public/images/logos/ before the executor runs TrustStrip.astro (Plan 02). While Plan 01 does not use logos, confirm with Scott now so they are ready before Plan 02 executes.

    Required files (commit to public/images/logos/ in your repo):
    - uperform.svg (or uperform.png — ≤50 KB)
    - ancile-solutions.svg (or ancile-solutions.png — ≤50 KB)
    - heycounsel.svg (or heycounsel.png — ≤50 KB)
    - inhouseed.svg (or inhouseed.png — ≤50 KB)

    If any logo is unavailable, that is fine — the executor will fall back to styled text (Inter 600, 14px, color ink-muted) showing the firm name. Just confirm which files exist or which need fallbacks so the executor knows what to expect.

    Note: public/images/logos/ does not exist yet. Running `mkdir -p public/images/logos` in your terminal creates it.
  </how-to-verify>
  <resume-signal>Type "logos ready" (with the filenames you committed) OR "use fallback text" (if no logos are available yet). Either answer unblocks Plan 01 execution.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Update Header.astro — nav links, CTA button, mobile hamburger</name>

  <read_first>
    - src/components/layout/Header.astro (current state — full file must be read before rewriting)
    - src/lib/constants.ts (FIRM.shortName, FIRM.email — import paths and field names)
    - src/styles/global.css (confirm token names: bg-surface, border-border, text-ink, bg-brand-blue-500, hover:bg-brand-blue-600)
    - .planning/phases/02-homepage-conversion-spine/02-UI-SPEC.md §Header (layout contract, hamburger behavior, aria requirements)
  </read_first>

  <files>src/components/layout/Header.astro</files>

  <action>
Fully replace the contents of src/components/layout/Header.astro with the implementation below. Read the current file first to understand what changes.

**What changes from Phase 1:**
- Removes the `mailto:` CTA link (replaced by a real `/contact` link with "Book a Fit Call" text)
- Adds `<nav aria-label="Main navigation">` with three real nav links
- Adds a hamburger `<button>` for mobile (hidden on desktop)
- Adds a mobile nav panel (`id="mobile-nav"`) that toggles open/closed
- Makes the header sticky (`sticky top-0 z-50`)
- Adds a vanilla `<script>` block for the hamburger toggle (zero JS libraries)

**Exact implementation to write:**

```astro
---
import { FIRM } from '../../lib/constants';
---
<header class="sticky top-0 z-50 bg-surface border-b border-border shadow-sm">
  <div class="mx-auto max-w-[1200px] px-md md:px-lg lg:px-xl h-[64px] md:h-[80px] flex items-center justify-between">

    <!-- Logo / Wordmark -->
    <a
      href="/"
      aria-label={`${FIRM.shortName} — Home`}
      class="font-display font-semibold text-[20px] md:text-[24px] text-ink hover:text-brand-blue-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500 transition-colors"
    >
      {FIRM.shortName}
    </a>

    <!-- Desktop nav + CTA (hidden on mobile) -->
    <nav aria-label="Main navigation" class="hidden md:flex items-center gap-8">
      <a href="/practice-areas" class="text-base text-ink hover:text-brand-blue-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500 transition-colors">Practice Areas</a>
      <a href="/attorneys/scott-palmer" class="text-base text-ink hover:text-brand-blue-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500 transition-colors">Scott Palmer</a>
      <a href="/pricing" class="text-base text-ink hover:text-brand-blue-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500 transition-colors">Pricing</a>
      <a
        href="/contact"
        class="bg-brand-blue-500 hover:bg-brand-blue-600 text-white text-base font-semibold py-3 px-6 rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-brand-blue-500 focus:ring-offset-2"
      >
        Book a Fit Call
      </a>
    </nav>

    <!-- Mobile hamburger button (hidden on desktop) -->
    <button
      id="hamburger"
      type="button"
      aria-controls="mobile-nav"
      aria-expanded="false"
      aria-label="Open navigation menu"
      class="md:hidden p-3 text-ink hover:text-ink-muted focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500"
    >
      <!-- 3-bar hamburger icon — 20px wide, 2px strokes, 6px gap -->
      <svg width="20" height="14" viewBox="0 0 20 14" fill="none" aria-hidden="true" focusable="false">
        <rect y="0"  width="20" height="2" rx="1" fill="currentColor"/>
        <rect y="6"  width="20" height="2" rx="1" fill="currentColor"/>
        <rect y="12" width="20" height="2" rx="1" fill="currentColor"/>
      </svg>
    </button>
  </div>

  <!-- Mobile nav panel (starts hidden; toggled by <script> below) -->
  <div
    id="mobile-nav"
    class="hidden md:hidden bg-surface border-b border-border"
    role="navigation"
    aria-label="Mobile navigation"
  >
    <div class="mx-auto max-w-[1200px] px-md py-6 flex flex-col gap-6">
      <a href="/practice-areas" class="text-base text-ink hover:text-brand-blue-500 py-[12px] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500">Practice Areas</a>
      <a href="/attorneys/scott-palmer" class="text-base text-ink hover:text-brand-blue-500 py-[12px] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500">Scott Palmer</a>
      <a href="/pricing" class="text-base text-ink hover:text-brand-blue-500 py-[12px] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500">Pricing</a>
      <a
        href="/contact"
        class="block w-full text-center bg-brand-blue-500 hover:bg-brand-blue-600 text-white text-base font-semibold py-3 px-6 rounded-lg transition-colors focus:outline-none focus:ring-2 focus:ring-brand-blue-500 focus:ring-offset-2"
      >
        Book a Fit Call
      </a>
    </div>
  </div>
</header>

<script>
  const hamburger = document.getElementById('hamburger') as HTMLButtonElement | null;
  const mobileNav = document.getElementById('mobile-nav') as HTMLElement | null;

  if (hamburger && mobileNav) {
    hamburger.addEventListener('click', () => {
      const isOpen = hamburger.getAttribute('aria-expanded') === 'true';
      hamburger.setAttribute('aria-expanded', String(!isOpen));
      hamburger.setAttribute(
        'aria-label',
        isOpen ? 'Open navigation menu' : 'Close navigation menu'
      );
      mobileNav.classList.toggle('hidden');
    });

    // Close on Escape key (accessibility requirement per UI-SPEC)
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && hamburger.getAttribute('aria-expanded') === 'true') {
        hamburger.setAttribute('aria-expanded', 'false');
        hamburger.setAttribute('aria-label', 'Open navigation menu');
        mobileNav.classList.add('hidden');
        hamburger.focus();
      }
    });
  }
</script>
```

**Why these exact choices:**
- `sticky top-0 z-50` — UI-SPEC §Header: sticky header
- `shadow-sm` — UI-SPEC: box-shadow when scrolled (simplified to static shadow)
- `aria-expanded="false"` on button + toggled by script — UI-SPEC §Mobile hamburger
- `aria-controls="mobile-nav"` + `id="mobile-nav"` — WCAG 2.1 AA aria pattern
- `aria-label="Main navigation"` on `<nav>` — UI-SPEC §Accessibility item 3
- Three nav links: Practice Areas → /practice-areas, Scott Palmer → /attorneys/scott-palmer, Pricing → /pricing — per CONTEXT.md D-15
- CTA "Book a Fit Call" → /contact — per CONTEXT.md D-12, D-14 (never "Contact Us")
- `py-3 px-6 rounded-lg` on CTA button — matches UI-SPEC §Button Variants (primary CTA)
- The `<script>` block uses vanilla JS only — per D12 (zero JS frameworks in v1)
- Escape key closes menu — UI-SPEC §Mobile hamburger behavior (accessibility requirement)
  </action>

  <verify>
    <automated>
      grep -c 'aria-label="Main navigation"' /Users/spalmer/Documents/Claude\ Code/Scopal\ Website/src/components/layout/Header.astro &&
      grep -c 'Book a Fit Call' /Users/spalmer/Documents/Claude\ Code/Scopal\ Website/src/components/layout/Header.astro &&
      grep -c 'Practice Areas' /Users/spalmer/Documents/Claude\ Code/Scopal\ Website/src/components/layout/Header.astro &&
      grep -c 'Scott Palmer' /Users/spalmer/Documents/Claude\ Code/Scopal\ Website/src/components/layout/Header.astro &&
      grep -c 'Pricing' /Users/spalmer/Documents/Claude\ Code/Scopal\ Website/src/components/layout/Header.astro &&
      grep -c 'aria-expanded' /Users/spalmer/Documents/Claude\ Code/Scopal\ Website/src/components/layout/Header.astro &&
      grep -c 'id="mobile-nav"' /Users/spalmer/Documents/Claude\ Code/Scopal\ Website/src/components/layout/Header.astro &&
      grep -c 'sticky top-0' /Users/spalmer/Documents/Claude\ Code/Scopal\ Website/src/components/layout/Header.astro
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep 'aria-label="Main navigation"' src/components/layout/Header.astro` → finds the nav element
    - `grep 'Book a Fit Call' src/components/layout/Header.astro` → appears exactly twice (desktop + mobile nav)
    - `grep 'aria-expanded="false"' src/components/layout/Header.astro` → finds the hamburger button default state
    - `grep 'id="mobile-nav"' src/components/layout/Header.astro` → finds the mobile panel
    - `grep '/contact' src/components/layout/Header.astro` → CTA links point to /contact (not mailto:)
    - `grep 'sticky top-0' src/components/layout/Header.astro` → header is sticky
    - `grep 'Contact Us' src/components/layout/Header.astro` → returns no matches (banned string)
    - `grep 'Free Consultation' src/components/layout/Header.astro` → returns no matches (banned string)
    - `grep 'Schedule a Consultation' src/components/layout/Header.astro` → returns no matches (Phase 1 CTA removed)
  </acceptance_criteria>

  <done>Header has sticky positioning, three real nav links, "Book a Fit Call" CTA linking to /contact, and a working hamburger with aria-expanded, aria-controls, and Escape-key close behavior. No banned strings present.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/pages/contact.astro — placeholder contact page</name>

  <read_first>
    - src/layouts/MarketingLayout.astro (confirm the Props interface and import path)
    - src/lib/constants.ts (FIRM.email — used in the mailto link; never hardcode the email address)
    - .planning/phases/02-homepage-conversion-spine/02-UI-SPEC.md §Contact Placeholder Page (layout spec, H1, page title, meta description)
  </read_first>

  <files>src/pages/contact.astro</files>

  <action>
Create a new file at src/pages/contact.astro. This page does NOT exist yet — create it from scratch.

This is a placeholder page. The real contact form arrives in Phase 4. For now it gives visitors a direct path to reach Scott while CTAs across the site have a working destination.

**Exact implementation to write:**

```astro
---
import MarketingLayout from '../layouts/MarketingLayout.astro';
import { FIRM } from '../lib/constants';
---
<MarketingLayout
  title="Contact — Annandale, NJ | Scopal Firm"
  description="Reach out to Scott Palmer at Scopal Firm to start a conversation about fractional general counsel for your SaaS company."
>
  <div class="mx-auto max-w-prose px-md py-12 md:py-16">
    <h1 class="font-display font-semibold text-4xl md:text-5xl text-ink leading-[1.15] mb-8">
      Get in Touch
    </h1>
    <p class="text-base leading-relaxed text-ink mb-6">
      The full contact form is on its way. In the meantime, reach out directly — Scott reads every message and responds within 2 business days.
    </p>
    <p class="text-base leading-relaxed text-ink">
      Email Scott at{' '}
      <a
        href={`mailto:${FIRM.email}`}
        class="text-ink underline underline-offset-2 hover:text-brand-blue-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue-500 transition-colors"
      >
        {FIRM.email}
      </a>.
    </p>
  </div>
</MarketingLayout>
```

**Why these exact choices:**
- `title="Contact — Annandale, NJ | Scopal Firm"` — exact string locked in CONTEXT.md and UI-SPEC §Contact
- `description="Reach out to Scott Palmer..."` — exact string locked in CONTEXT.md and UI-SPEC §Contact (≤155 chars ✓)
- `<h1>Get in Touch</h1>` — exact H1 copy locked in CONTEXT.md D-11 and UI-SPEC
- `FIRM.email` in the mailto href and link text — NAP single-source rule (never hardcode email)
- `max-w-prose` — UI-SPEC §Contact: single centered column, max-w-prose
- `py-12 md:py-16` — approximates the UI-SPEC `padding: 2xl top, xl bottom` (48px/32px) within Tailwind classes
- Body copy is exactly as specified in the planning_guidance §Contact page body
- No CTA button — UI-SPEC §Contact: "No CTA button — the mailto link IS the CTA on this placeholder page"
- `MarketingLayout` without `emitOrgSchema` — LegalService schema is homepage-only per D8
  </action>

  <verify>
    <automated>
      cd /Users/spalmer/Documents/Claude\ Code/Scopal\ Website && npm run build 2>&1 | tail -5 && test -f dist/contact/index.html && echo "PASS: dist/contact/index.html exists" || echo "FAIL: build or file missing"
    </automated>
  </verify>

  <acceptance_criteria>
    - `test -f src/pages/contact.astro` → file exists
    - `grep 'Get in Touch' src/pages/contact.astro` → H1 contains exact string
    - `grep 'Contact — Annandale, NJ | Scopal Firm' src/pages/contact.astro` → exact page title present
    - `grep 'FIRM.email' src/pages/contact.astro` → email comes from constants, not hardcoded
    - `grep 'scott@scopalfirm.com' src/pages/contact.astro` → returns no matches (email must not be hardcoded)
    - `grep 'MarketingLayout' src/pages/contact.astro` → correct layout used
    - `grep 'emitOrgSchema' src/pages/contact.astro` → returns no matches (LegalService schema is homepage-only)
    - `npm run build` exits 0
    - `test -f dist/contact/index.html` → Astro built the /contact route
    - `grep 'Get in Touch' dist/contact/index.html` → H1 rendered in the built output
    - `grep 'Contact Us' src/pages/contact.astro` → returns no matches (banned CTA string)
    - `grep 'Free Consultation' src/pages/contact.astro` → returns no matches (banned CTA string)
  </acceptance_criteria>

  <done>A visitor navigating to /contact sees an H1 "Get in Touch", a brief note that the form is coming, and a mailto link to scott@scopalfirm.com. The page title and meta description match the locked spec exactly. npm run build exits 0 and dist/contact/index.html exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → static page | All pages in Phase 2 are pre-built static HTML; no server-side input processing occurs |
| hamburger script → DOM | Vanilla JS reads/writes aria attributes and CSS classes; no external input, no eval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | mailto link (contact.astro) | accept | Static mailto link; no form input, no server processing; email address is public on the site by design |
| T-02-02 | Information Disclosure | Header.astro script block | accept | Script only toggles aria-expanded and hidden class; no data read, no network calls, no secrets |
| T-02-03 | Tampering | Static HTML in dist/ | accept | Static files served by Vercel CDN; no runtime mutation possible; CSP from vercel.json applies |
</threat_model>

<verification>
After both tasks complete, verify the following from the project root:

```bash
# 1. Header has required aria and copy strings
grep 'aria-label="Main navigation"' src/components/layout/Header.astro
grep 'Book a Fit Call' src/components/layout/Header.astro
grep 'aria-expanded' src/components/layout/Header.astro
grep 'id="mobile-nav"' src/components/layout/Header.astro

# 2. Banned strings absent from header
grep -v '^#' src/components/layout/Header.astro | grep -c 'Contact Us' || true   # must be 0
grep -v '^#' src/components/layout/Header.astro | grep -c 'Free Consultation' || true  # must be 0

# 3. Contact page built successfully
npm run build
test -f dist/contact/index.html && echo "contact page built OK"
grep 'Get in Touch' dist/contact/index.html

# 4. Banned words CI passes
npm run banned-words
```
</verification>

<success_criteria>
- Header is sticky, has "Main navigation" aria-label, three real nav links, "Book a Fit Call" CTA linking to /contact, and a working hamburger menu with proper aria attributes
- Mobile nav closes on Escape key press
- /contact page renders with H1 "Get in Touch", mailto link using FIRM.email, exact page title and meta description
- npm run build exits 0 — no TypeScript errors, no broken imports
- dist/contact/index.html exists in build output
- npm run banned-words passes — no banned vocabulary in new files
</success_criteria>

<output>
After completing all tasks, create a summary file at:
.planning/phases/02-homepage-conversion-spine/02-01-SUMMARY.md

The summary should record:
- What was built (Header update details, contact page details)
- Any deviations from the plan (e.g., if logo files were not ready)
- Any Tailwind token names that behaved unexpectedly
- Build output confirmation

Plain English for Scott: "This plan updates the site's navigation bar so it has real links and a 'Book a Fit Call' button, and creates a placeholder page at /contact so that button has somewhere to go."
</output>
