---
phase: "04-lead-capture-content-launch-hardening"
plan: "04-00"
type: execute
wave: 0
depends_on: []
autonomous: true
files_modified:
  - public/fonts/Inter-Regular.woff2
  - public/fonts/Inter-SemiBold.woff2
  - public/fonts/Fraunces-SemiBold.woff2
  - public/og-default.jpg
  - src/components/legal/Testimonial.astro
  - src/components/legal/CaseResult.astro
  - src/content/blog/when-saas-needs-a-lawyer.mdx
requirements:
  - PERF-02
  - SEO-03
  - LEGAL-04
  - BLOG-05

must_haves:
  truths:
    - "Three woff2 font files exist in public/fonts/ (Inter-Regular, Inter-SemiBold, Fraunces-SemiBold)"
    - "public/og-default.jpg exists at 1200x630 and is ≤200 KB"
    - "Testimonial.astro and CaseResult.astro each require a 'disclaimer' prop — build fails without it"
    - "At least one published blog post MDX file exists in src/content/blog/"
  artifacts:
    - path: "public/fonts/Inter-Regular.woff2"
      provides: "Inter 400 self-hosted font"
    - path: "public/fonts/Inter-SemiBold.woff2"
      provides: "Inter 600 self-hosted font"
    - path: "public/fonts/Fraunces-SemiBold.woff2"
      provides: "Fraunces 600 self-hosted font"
    - path: "public/og-default.jpg"
      provides: "Default OG image 1200x630"
    - path: "src/components/legal/Testimonial.astro"
      provides: "Testimonial component with required disclaimer prop"
      exports: ["default"]
    - path: "src/components/legal/CaseResult.astro"
      provides: "CaseResult component with required disclaimer prop"
      exports: ["default"]
    - path: "src/content/blog/when-saas-needs-a-lawyer.mdx"
      provides: "First published blog post"
      contains: "draft: false"
  key_links:
    - from: "src/styles/global.css"
      to: "public/fonts/Inter-Regular.woff2"
      via: "@font-face src url"
      pattern: "url\\('/fonts/Inter-Regular.woff2'\\)"
    - from: "src/layouts/BaseLayout.astro"
      to: "public/fonts/Inter-Regular.woff2"
      via: "rel=preload link tag"
      pattern: "preload.*Inter-Regular.woff2"
---

<objective>
Create the prerequisite assets that every other Phase 4 plan depends on: self-hosted font files, the default OG image, stub legal components, and the first blog post.

Purpose: Wave 0 because every subsequent plan's `npm run build` and Lighthouse tests require these assets to exist. If fonts are missing, Lighthouse will flag FOUT/FOIT. If OG image is missing, SEO.astro upgrade (04-02) has nothing to fall back to. If the first blog post MDX doesn't exist, the blog build (04-01) produces an empty index.

Output: 3 woff2 files, 1 JPEG, 2 legal components, 1 MDX post, @font-face rules in global.css, preload links in BaseLayout.astro.
</objective>

<execution_context>
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/phases/04-lead-capture-content-launch-hardening/../../../.claude/CLAUDE.md
</execution_context>

<context>
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/ROADMAP.md
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/FIRM_BRIEF.md
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/phases/04-lead-capture-content-launch-hardening/04-RESEARCH.md
@/Users/spalmer/Documents/Claude Code/Scopal Website/.planning/phases/04-lead-capture-content-launch-hardening/04-PATTERNS.md

<interfaces>
<!-- From src/lib/constants.ts (shared source of truth) -->
```typescript
export const FIRM = {
  legalName: "Scopal Firm, LLC",
  email: "scott@scopalfirm.com",
  url: "https://scopalfirm.com",
  responsibleAttorney: "Scott A. Palmer",
  noAttorneyClientDisclaimer: string,   // full disclaimer text
  attorneyAdvertising: string,          // "Attorney Advertising. Prior results do not guarantee a similar outcome."
  jurisdictionDisclaimer: string,       // full jurisdiction text
};
```

<!-- From src/components/legal/DisclaimerCallout.astro (analog for Testimonial/CaseResult) -->
```astro
---
import { FIRM } from '../../lib/constants';
---
<div class="border-l-4 border-brand-blue-500 bg-surface-muted px-6 py-4 text-sm text-ink-muted leading-relaxed rounded-r-lg" role="note" aria-label="Legal disclaimer">
  <strong class="text-ink">Note:</strong> {FIRM.noAttorneyClientDisclaimer}
</div>
```

<!-- From src/content.config.ts (blog schema — confirms available frontmatter fields) -->
```typescript
// blog collection schema includes:
// title: z.string().max(70)
// description: z.string().max(160)
// pubDate: z.date()
// updatedDate: z.date().optional()
// author: z.enum(['scott-palmer'])
// tags: z.array(z.string()).default([])
// draft: z.boolean().default(false)
// heroImage: z.string().optional()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download and commit self-hosted font files + wire @font-face + preload links</name>
  <files>
    public/fonts/Inter-Regular.woff2
    public/fonts/Inter-SemiBold.woff2
    public/fonts/Fraunces-SemiBold.woff2
    src/styles/global.css
    src/layouts/BaseLayout.astro
  </files>
  <read_first>
    - src/styles/global.css (full file — need line count and structure to know where to append @font-face)
    - src/layouts/BaseLayout.astro (full file — find the TODO comment on the preload link, line ~25)
  </read_first>
  <action>
**Step 1: Download font files via npm Fontsource packages (do NOT use CDN links)**

Run these commands (they download the woff2 subsets without adding runtime dependencies):

```bash
# Install Fontsource packages temporarily to extract woff2 files
cd "/Users/spalmer/Documents/Claude Code/Scopal Website"
npm install --save-dev @fontsource/inter @fontsource/fraunces
```

Then copy the specific woff2 files:

```bash
# Inter Regular (400)
cp node_modules/@fontsource/inter/files/inter-latin-400-normal.woff2 public/fonts/Inter-Regular.woff2

# Inter SemiBold (600)
cp node_modules/@fontsource/inter/files/inter-latin-600-normal.woff2 public/fonts/Inter-SemiBold.woff2

# Fraunces SemiBold (600) — Fraunces is a display font; use the normal style
cp node_modules/@fontsource/fraunces/files/fraunces-latin-600-normal.woff2 public/fonts/Fraunces-SemiBold.woff2
```

After copying, verify each file is ≤200 KB:
```bash
ls -lh public/fonts/
```

If any file exceeds 200 KB (unlikely — Inter latin subset is ~65 KB, Fraunces ~80 KB), use the `--subset=latin` variant or download from google-webfonts-helper instead.

**Step 2: Add @font-face rules to global.css**

Read global.css to find the end of the `@layer base` block, then APPEND the following @font-face declarations AFTER the closing `}` of `@layer base` (after the last line of the file). Do NOT insert inside `@theme` or `@layer base`.

```css
/* SELF-HOSTED FONTS — Phase 4 / PERF-02
 * Files committed to public/fonts/. No Google Fonts CDN.
 * font-display: swap per D10. Two families × two weights (D10 constraint).
 */
@font-face {
  font-family: "Inter";
  font-style: normal;
  font-weight: 400;
  font-display: swap;
  src: url('/fonts/Inter-Regular.woff2') format('woff2');
}

@font-face {
  font-family: "Inter";
  font-style: normal;
  font-weight: 600;
  font-display: swap;
  src: url('/fonts/Inter-SemiBold.woff2') format('woff2');
}

@font-face {
  font-family: "Fraunces";
  font-style: normal;
  font-weight: 600;
  font-display: swap;
  src: url('/fonts/Fraunces-SemiBold.woff2') format('woff2');
}
```

**Step 3: Update BaseLayout.astro preload links**

Find the TODO comment in BaseLayout.astro (approximately line 25):
```
<!-- TODO: Add Fraunces 600 preload link once woff2 file is committed to public/fonts/ -->
```

Replace that TODO comment line with these two preload link tags:
```astro
<link rel="preload" as="font" type="font/woff2" href="/fonts/Inter-Regular.woff2" crossorigin />
<link rel="preload" as="font" type="font/woff2" href="/fonts/Fraunces-SemiBold.woff2" crossorigin />
```

Note: These preload links must come BEFORE the `<SEO .../>` component in the `<head>` to maximize render benefit. The TODO comment is already in the right position.
  </action>
  <verify>
    <automated>ls -lh "/Users/spalmer/Documents/Claude Code/Scopal Website/public/fonts/" && grep -n "font-face" "/Users/spalmer/Documents/Claude Code/Scopal Website/src/styles/global.css" && grep -n "preload.*Inter-Regular" "/Users/spalmer/Documents/Claude Code/Scopal Website/src/layouts/BaseLayout.astro"</automated>
  </verify>
  <done>
    - public/fonts/ contains Inter-Regular.woff2, Inter-SemiBold.woff2, Fraunces-SemiBold.woff2 (each ≤200 KB)
    - global.css contains three @font-face blocks (Inter 400, Inter 600, Fraunces 600) with font-display: swap
    - BaseLayout.astro head contains two rel=preload links for Inter-Regular and Fraunces-SemiBold
    - TODO comment removed from BaseLayout.astro
  </done>
</task>

<task type="auto">
  <name>Task 2: Create default OG image + Testimonial/CaseResult stub components + first blog post MDX</name>
  <files>
    public/og-default.jpg
    src/components/legal/Testimonial.astro
    src/components/legal/CaseResult.astro
    src/content/blog/when-saas-needs-a-lawyer.mdx
  </files>
  <read_first>
    - src/components/legal/DisclaimerCallout.astro (analog for Testimonial and CaseResult — copy class patterns)
    - src/content.config.ts (blog frontmatter schema — confirm field names and constraints)
  </read_first>
  <action>
**Step 1: Generate the default OG image**

The OG image must be exactly 1200×630 px, JPEG format, ≤200 KB. Create it using a Node.js canvas approach or any available image generation tool. If no canvas tooling is available, generate a minimal branded JPEG using ImageMagick or the `sharp` package (already a transitive dependency of @astrojs/vercel).

Suggested approach using sharp:
```bash
cd "/Users/spalmer/Documents/Claude Code/Scopal Website"
node -e "
const sharp = require('sharp');
const svg = \`<svg width='1200' height='630' xmlns='http://www.w3.org/2000/svg'>
  <rect width='1200' height='630' fill='#0f172a'/>
  <text x='80' y='280' font-family='Georgia, serif' font-size='72' font-weight='bold' fill='white'>Scopal Firm, LLC</text>
  <text x='80' y='380' font-family='Georgia, serif' font-size='36' fill='#94a3b8'>Fractional General Counsel for SaaS Companies</text>
  <text x='80' y='560' font-family='Georgia, serif' font-size='24' fill='#64748b'>scopalfirm.com</text>
</svg>\`;
sharp(Buffer.from(svg)).jpeg({ quality: 85 }).toFile('public/og-default.jpg', (err, info) => {
  if (err) throw err;
  console.log('OG image created:', info);
});
"
```

If sharp is not available as a direct import, try:
```bash
node -e "const s = require('./node_modules/sharp'); ..." 
```

Verify the output: `ls -lh public/og-default.jpg` — must show file exists and be ≤200 KB.

**Step 2: Create Testimonial.astro**

Create `src/components/legal/Testimonial.astro` with the exact content below. The `disclaimer` prop is REQUIRED — TypeScript enforces this at build time, satisfying LEGAL-04.

```astro
---
interface Props {
  quote: string;
  attribution: string;
  disclaimer: string;
}
const { quote, attribution, disclaimer } = Astro.props;
---
<figure class="border-l-4 border-brand-blue-500 bg-surface-muted px-6 py-4 rounded-r-lg">
  <blockquote class="text-base leading-relaxed text-ink italic">
    <p>{quote}</p>
  </blockquote>
  <figcaption class="mt-2 text-sm text-ink-muted">— {attribution}</figcaption>
  <div
    class="mt-3 text-sm text-ink-muted leading-relaxed"
    role="note"
    aria-label="Legal disclaimer"
  >
    <strong class="text-ink">Note:</strong> {disclaimer}
  </div>
</figure>
```

**Step 3: Create CaseResult.astro**

Create `src/components/legal/CaseResult.astro` with the exact content below. The `disclaimer` prop is REQUIRED — same LEGAL-04 enforcement.

```astro
---
interface Props {
  outcome: string;
  context: string;
  disclaimer: string;
}
const { outcome, context, disclaimer } = Astro.props;
---
<div class="bg-surface border border-border rounded-lg p-6">
  <p class="font-display font-semibold text-xl text-ink leading-[1.25]">{outcome}</p>
  <p class="mt-2 text-base leading-relaxed text-ink-muted">{context}</p>
  <div
    class="mt-4 border-l-4 border-brand-blue-500 px-4 py-3 text-sm text-ink-muted leading-relaxed rounded-r-lg bg-surface-muted"
    role="note"
    aria-label="Legal disclaimer"
  >
    <strong class="text-ink">Note:</strong> {disclaimer}
  </div>
</div>
```

**Step 4: Create the first blog post MDX**

Create `src/content/blog/when-saas-needs-a-lawyer.mdx` with the frontmatter and body below.

CRITICAL rules:
- Do NOT use banned vocabulary: "specialist," "specializing in," "expert," "experts," "the best," "#1," "leading," "top-rated," "super lawyer"
- DO use: "focuses on," "represents clients in," "experienced in"
- `pubDate` must match today's date or a realistic publish date: use `2026-05-08`
- `author` must be `scott-palmer` (matches the Zod enum in content.config.ts)
- `draft: false` (or omit since schema defaults to false)
- Title max 70 chars, description max 160 chars

```mdx
---
title: "When Does a SaaS Company Need a Lawyer?"
description: "Most SaaS founders wait too long to get legal help. Here's how to know when it's time — and what kind of support makes sense at each stage."
pubDate: 2026-05-08
author: scott-palmer
tags: ["saas", "startup-legal", "fractional-gc"]
draft: false
---

Most SaaS founders treat legal the same way they treat dentist appointments — they only go when something hurts. By then, the problem is usually bigger (and more expensive) than it needed to be.

Here's a practical guide to the moments that signal your company is ready for a legal partner — and what to look for when you start that search.

## The Moment That Catches Most Founders Off Guard: The First Big Contract

A potential enterprise customer sends over their Master Service Agreement. It's 47 pages. Your VP of Sales wants to close by end of quarter.

You can sign it as-is (risky), mark it up yourself (risky and time-consuming), or hire an outside firm to redline it (expensive — often $3,000–$8,000 for a single contract review).

This is the moment most SaaS founders first call a lawyer. But if you already had a legal partner who knows your product, your data practices, and your standard contract positions, this wouldn't be a crisis — it would be a Tuesday.

## Five Signs It's Time to Bring Legal In

### 1. You're Closing Contracts Over $50K Annually

Below this threshold, standard contracts with light negotiation are usually fine. Above it, enterprise buyers have legal teams reviewing your terms. You need someone in your corner.

### 2. You're Handling Customer Data Across State Lines (or Internationally)

Data privacy law — CCPA, GDPR, state biometric laws — is not optional. If you're collecting personal data from users in California, Virginia, Colorado, or the EU, you have compliance obligations whether or not you're aware of them.

A lawyer who focuses on SaaS clients can review your data practices, DPA templates, and privacy policy in a few hours. The alternative is discovering your exposure when a regulator (or plaintiffs' attorney) finds it first.

### 3. You're Taking Venture or Angel Investment

An investor's counsel will protect the investor. You need independent representation to review term sheets, cap table mechanics, and investor rights. This is not the time to lean on a friend who "knows some contract stuff."

### 4. You're Adding Employees or Contractors

The moment you hire someone — W-2 or 1099 — you have employment law exposure. Misclassification, offer letter terms, non-compete enforceability, IP assignment agreements. These matters are much cheaper to handle correctly upfront than to fix after a dispute.

### 5. You're Using AI in Your Product

If your product uses large language models, AI-generated content, or automated decision-making, you're operating in one of the fastest-moving areas of law. Customers are asking about AI governance. Enterprise buyers want AI addenda in their contracts. Regulators are paying attention.

This is an area where experienced-in matters — and where a lawyer who is also AI-native can add real value.

## What Kind of Legal Support Makes Sense at Each Stage

**Pre-revenue / bootstrapped:** At this stage, prioritize incorporation, a basic IP assignment agreement, and a standard contractor agreement. One-time flat-fee work. Cost: $1,500–$3,000.

**Seed-stage / $500K–$3M ARR:** You're starting to close real contracts and may be taking investor money. A fractional GC relationship starts to make economic sense here — available consistently, knows your business, costs less than a law firm associate's hourly rate on a retainer basis.

**Series A and beyond:** At this stage, the question is usually whether to hire in-house full-time. A fractional GC can fill the gap — or help you determine when a full-time hire actually makes sense.

## The Real Question: Reactive vs. Proactive

Here's what I've seen over seven years as a General Counsel at a PE-backed SaaS company: the companies that treat legal as a reactive expense spend more than the companies that treat it as an ongoing partnership.

A lawyer who knows your business can spot a problem in a contract in 20 minutes. A lawyer who doesn't know your business charges you for the time it takes to get up to speed — every single time.

The math usually favors consistency.

---

*Scott Palmer represents clients in corporate law, commercial contracting, and fractional general counsel engagements. Scopal Firm focuses on SaaS companies that want dedicated legal support without the overhead of a full-time hire. [Get in touch](/contact) if you'd like to talk about your situation.*
```
  </action>
  <verify>
    <automated>ls -lh "/Users/spalmer/Documents/Claude Code/Scopal Website/public/og-default.jpg" && grep -n "disclaimer" "/Users/spalmer/Documents/Claude Code/Scopal Website/src/components/legal/Testimonial.astro" && grep -n "disclaimer" "/Users/spalmer/Documents/Claude Code/Scopal Website/src/components/legal/CaseResult.astro" && grep -n "draft" "/Users/spalmer/Documents/Claude Code/Scopal Website/src/content/blog/when-saas-needs-a-lawyer.mdx"</automated>
  </verify>
  <done>
    - public/og-default.jpg exists, is JPEG format, dimensions 1200x630, ≤200 KB
    - src/components/legal/Testimonial.astro exists with required `disclaimer` prop in interface (not optional — no `?`)
    - src/components/legal/CaseResult.astro exists with required `disclaimer` prop in interface (not optional — no `?`)
    - src/content/blog/when-saas-needs-a-lawyer.mdx exists with draft: false, author: scott-palmer, pubDate: 2026-05-08
    - Blog post content contains no banned words (run: npm run banned-words)
    - npm run build completes without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| font files → browser | Static binary assets served over HTTPS; no trust boundary concern |
| MDX content → build pipeline | Content is author-controlled and built statically; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Information Disclosure | public/fonts/*.woff2 | accept | Font files are intentionally public; no sensitive data; OFL license permits hosting |
| T-04-00-02 | Tampering | src/content/blog/*.mdx | accept | MDX content is committed to git; gitleaks CI prevents secrets; banned-words CI prevents compliance violations |
</threat_model>

<verification>
After both tasks complete:

1. `ls -lh "/Users/spalmer/Documents/Claude Code/Scopal Website/public/fonts/"` — three woff2 files, each ≤200 KB
2. `ls -lh "/Users/spalmer/Documents/Claude Code/Scopal Website/public/og-default.jpg"` — exists, ≤200 KB
3. `grep -c "font-face" "/Users/spalmer/Documents/Claude Code/Scopal Website/src/styles/global.css"` — returns 3
4. `grep -n "preload" "/Users/spalmer/Documents/Claude Code/Scopal Website/src/layouts/BaseLayout.astro"` — shows two preload link lines (no TODO comment)
5. `grep -n "disclaimer: string" "/Users/spalmer/Documents/Claude Code/Scopal Website/src/components/legal/Testimonial.astro"` — shows required prop (no `?`)
6. `grep -n "disclaimer: string" "/Users/spalmer/Documents/Claude Code/Scopal Website/src/components/legal/CaseResult.astro"` — shows required prop (no `?`)
7. `grep -n "author: scott-palmer" "/Users/spalmer/Documents/Claude Code/Scopal Website/src/content/blog/when-saas-needs-a-lawyer.mdx"` — confirms attribution
8. `cd "/Users/spalmer/Documents/Claude Code/Scopal Website" && npm run banned-words` — passes (0 violations)
9. `cd "/Users/spalmer/Documents/Claude Code/Scopal Website" && npm run build` — completes without errors
</verification>

<success_criteria>
- Three self-hosted woff2 files committed to public/fonts/ (Inter 400, Inter 600, Fraunces 600), each ≤200 KB
- global.css has three @font-face declarations with font-display: swap
- BaseLayout.astro has preload links for Inter-Regular.woff2 and Fraunces-SemiBold.woff2 (TODO comment removed)
- public/og-default.jpg exists at 1200x630, ≤200 KB, JPEG format
- Testimonial.astro and CaseResult.astro each have a non-optional `disclaimer: string` prop — TypeScript build fails if prop is omitted at call site
- First blog post MDX exists with draft: false, author: scott-palmer, no banned vocabulary
- npm run build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-lead-capture-content-launch-hardening/04-00-SUMMARY.md` with:
- What was built (font files, OG image, legal components, first blog post)
- Exact file sizes of font files (from ls -lh output)
- Confirmation that banned-words and build both pass
- Any deviations from plan (e.g., if sharp was unavailable and a different OG image approach was used)
</output>
